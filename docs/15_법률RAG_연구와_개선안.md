# 15. 법률 도메인 RAG 최신 연구와 우리 검색기 개선안

> 작성 2026-08-20 · 대상 시스템 `F:/policy_maps/system` (`policymap.rag`)
> 이 문서의 모든 수치는 **본 라운드에서 직접 실행한 결과**다. 재현 방법은 §5에 적었다.
> 논문은 실제로 원문(arXiv/저널/공식 저장소)을 확인한 것만 인용했다.

---

## 0. 요약 (TL;DR)

직전 라운드에서 우리는 "하이브리드 검색이 BM25 단독을 이기지 못한다"는 반증을 얻었다.
이번 라운드에서 그 **원인을 코드 수준에서 특정**했고, 처방을 실측으로 검증했다.

**원인 한 줄:** 우리 "dense" 채널은 dense 가 아니다. `data/index/all/meta.json` 의
`model: "char-ngram-tf"`, `dense_kind: "sparse"`, `dense_dim: 0` 이 말해주듯, dense 채널은
**BM25 와 같은 문자 n-gram 자질 공간에서 IDF 없이 raw TF 코사인**을 계산한다.
즉 dense 는 BM25 의 독립적 보완 채널이 아니라 **IDF·길이정규화를 제거한 열화 복제본**이다.
서로 다른 신호를 섞어야 이득이 나는 융합(fusion)에 열화 복제본을 섞었으니,
하이브리드가 BM25 를 못 이기는 것은 **구조적 필연**이었다.

**그리고 처방이 실제로 통했다.** cross-encoder 재랭킹을 얹은 결과(28질의 실측):

| 지표 | BM25 (기준선) | +CrossEncoder 재랭킹 | 개선 |
|---|---:|---:|---:|
| P@5 | 0.4714 | **0.6643** | **+40.9%** |
| Success@5 | 0.6429 | **0.8214** | **+27.8%** |
| P@1 | 0.5357 | **0.7143** | **+33.3%** |

> **가장 중요한 한 줄:** BM25 top-50 후보군의 이론적 천장은 Success@50 = **0.8214** 인데,
> cross-encoder 재랭킹의 Success@5 가 **정확히 0.8214** 다.
> **후보군 안에 있던 정답을 100% top-5 로 끌어올렸다** — 재랭킹으로는 더 이상 짜낼 것이 없다.
> 남은 개선은 전부 **재현율(후보 생성)** 문제이며, 그것이 개선안 ①③이다.

**핵심 실측 4가지**

| 관측 | 수치 | 의미 |
|---|---|---|
| BM25 성공률 top-5 vs top-50 | 0.6429 → 0.8214 | +17.9pp 가 재랭킹으로 회수 가능 |
| Success@20 = Success@50 | 둘 다 0.8214 | 나머지 17.9pp 는 **후보군에 아예 없음** → 재현율 문제 |
| 현행 dense 단독 P@5 | 0.3071 (BM25 의 65%) | dense 채널이 명백히 열등 |
| BM25+Dense→재랭킹 < BM25→재랭킹 | 0.6500 < 0.6643 | 잡음 채널은 **후보 생성에도** 손해 |

**처방 우선순위:** ①한국어 임베딩 모델(KURE-v1)로 dense 채널 교체 → ②cross-encoder 재랭킹 →
③조문 chunking·문서맥락 주입 개선 → ④그래프는 검색 랭킹에서 빼고 "관련 조례 확장 제안" UI 로 재배치.

> **실행 순서는 우선순위와 다르다.** ②가 **오늘 당장 붙일 수 있고 효과가 검증되었으므로 먼저 배포**하고,
> ①③은 재색인이 필요하므로 뒤따른다. ①은 근본적이지만 CPU 비용이 크다(§6-①).

**가용성 확인:** 오프라인이 아니다. HuggingFace 도달 가능(HTTP 200)하며 아래 모델이
**실제로 다운로드·로드·추론되는 것을 확인**했다.

| 모델 | 역할 | 차원 | CPU 로드 | 실측 |
|---|---|---|---|---|
| `nlpai-lab/KURE-v1` | 한국어 검색 임베딩 | 1024 | 31.1s | OK |
| `BAAI/bge-m3` | 다국어 임베딩(KURE 의 베이스) | 1024 | 7.3s | OK |
| `jhgan/ko-sroberta-multitask` | 한국어 문장 임베딩 | 768 | 13.6s | OK |
| `BAAI/bge-reranker-v2-m3` | cross-encoder 리랭커 | – | 30.2s | OK |
| `Dongjin-kr/ko-reranker` | 한국어 리랭커 | – | 36.0s | OK |

설치 환경 실측: `torch 2.11.0+cpu`, `sentence-transformers 5.5.1`, `transformers 5.9.0`,
`kiwipiepy 0.23.0`(한국어 형태소분석기 — **이미 설치되어 있는데 안 쓰고 있다**),
`sklearn 1.8.0`, `numpy 2.4.3`. `faiss`·`konlpy`·`rank_bm25` 는 없으나 모두 불필요하다
(역색인을 직접 구현했으므로).

> **GPU 는 없다**(`+cpu` 빌드). 이 문서의 모든 비용 추정은 CPU 기준이다.

---

## 1. 문제 정의 — 직전 라운드의 반증

직전 라운드 실측(수작업 라벨 16질의):

- Recall@5: **BM25 0.814 > hybrid 0.776 > dense 0.653**
- MRR 은 4방식 모두 1.000 으로 포화 → 지표로서 무용
- `hybrid+graph` 는 16/16 질의에서 `hybrid` 와 **완전히 동일**(그래프 기여 0)

두 가지 실패가 겹쳐 있다. **(A) dense 를 섞으면 오히려 나빠진다. (B) 그래프를 섞으면 아무 일도 안 일어난다.**
아래에서 각각의 원인을 코드와 실측으로 특정한다.

---

## 2. 코드 수준 진단

### 2.1 "dense" 채널은 dense 가 아니다 — 결정적 증거

`data/index/all/meta.json` 실측 값:

```
model      : char-ngram-tf
tokenizer  : {'ngrams': [2, 3], 'max_chars': 4000}
n_docs     : 62460
avgdl      : 305.90
bm25       : {'k1': 1.2, 'b': 0.75}
segment_meta[0].dense_kind : 'sparse'
segment_meta[0].dense_dim  : 0
segment_meta[0].bm25       : {'terms': 335912, 'postings': 10801084}
segment_meta[0].dense      : {'terms': 655934, 'postings': 21583063}
```

`dense_dim: 0`, `dense_kind: 'sparse'` — **신경망 임베딩이 한 번도 쓰인 적이 없다.**

`policymap/parsers/embedding.py` 의 `Embedder` 는 기본값이 `char-ngram-tf` 이고,
`_char_ngram_tf()` 가 공백을 제거한 문자열에서 2·3-gram **빈도 사전**을 만든다.
`sbert:` 접두사를 주면 sentence-transformers 로 전환되는 경로가 이미 있으나
(`_try_load_sbert`), 인덱스는 그 경로를 쓰지 않고 빌드되었다.

문제의 핵심은 **가중치가 없다는 것**이다:

- BM25 채널: IDF × 문서길이 정규화(`k1=1.2, b=0.75`) 적용
- dense 채널: **raw TF 코사인** — IDF 없음, 길이정규화는 L2 뿐

그 결과 `예방`, `관리`, `운영`, `및 `, `에 관`, `한 조` 같은
**고빈도 무정보 n-gram 이 유사도를 지배**한다.

### 2.2 그 결과 — 재현된 오검색

같은 질의에 대한 두 채널의 top-5 (실행 출력 그대로):

**질의: "산사태 예방 및 복구"**

| # | BM25 | Dense (현행) |
|---|---|---|
| 1 | 하동군 산사태취약지역지정위원회 구성 및 운영에 관한 조례 | 아산시 유해야생동물 피해예방 및 관리 조례 |
| 2 | 의령군 산사태취약지역 지정위원회 구성 및 운영 조례 | 서울특별시 동대문구 유해야생동물 피해예방 및 관리 조례 |
| 3 | 고성군 산사태취약지역 지정위원회 구성 및 운영에 관한 조례 | 전남광주통합특별시 동구 유해야생동물 피해 예방 및 관리에 관한 조례 |
| 4 | 광명시 산사태취약지역지정위원회 구성 및 운영에 관한 조례 | 진안군 농업작업 안전재해 예방 및 지원에 관한 조례 |
| 5 | 고성군 산사태취약지역 지정위원회 구성 및 운영에 관한 조례 | 파주시 유해야생동물 먹이주기 금지 조례 |

BM25 는 정확히 맞히고, dense 는 **"예방 및 관리"라는 공통 껍데기만 보고** 전혀 다른 주제를 가져온다.
`산사태` 라는 결정적 저빈도 토큰이 IDF 가 없어 묻혀버린 것이다.

**질의: "공공 와이파이 설치 운영"** — dense top-1 은 `화천군 반려동물 복지 및 반려문화 조성 조례`다.
현재 본문 확보 코퍼스가 동물보호·반려동물 주제로 편중(§3.3 참조)되어 있어,
무가중 코사인이 **코퍼스 사전확률(prior)에 그대로 끌려간 것**이다.
BM25 도 이 질의에서는 `전기통신사업법`·`행정기구 설치 조례 시행규칙` 을 올려 좋지 않지만,
적어도 "설치/운영"이라는 질의 어휘와는 연결되어 있다.

### 2.3 두 채널은 얼마나 다른가 — 중복도 실측

16개 질의에 대해 두 채널 top-k 겹침을 측정했다.

```
MEAN Jaccard@20  = 0.4245
MEAN overlap@10  = 0.5125
```

질의별 편차가 크다 — `주민참여예산제 운영` overlap@10 = 1.00 (완전 동일),
`공공 와이파이 설치 운영`·`산사태 예방 및 복구`·`農漁民 수당` = 0.00 (완전 분리).

융합 이론상 이상적인 보완 채널은 "서로 다르되 **각자 옳아야**" 한다.
우리 dense 는 **다르기는 한데 그 다른 절반이 틀렸다**. 그래서 RRF 가 손해를 본다.
중복도가 1.0 인 질의에서는 dense 가 무해하지만 무익하고,
중복도가 0.0 인 질의에서는 dense 가 **순수한 잡음 주입기**로 작동한다.

### 2.4 RRF 산식상 그래프는 top-k 에 진입할 수 없다 (직전 발견 재확인)

`policymap/rag/retrieve.py:693` `hybrid_graph_search()`:

```python
sc += 1.0 / (rrf_k + m["search_rank"])
sc += graph_weight / (rrf_k + m["graph_rank"])
```

`rrf_k=60`, `graph_weight=0.5` 이므로 그래프 단독 노드의 **최대** 점수는
`0.5/(60+1) = 0.00820`. 반면 검색 히트의 10위 점수는 `1/(60+10) = 0.01429`.
`0.00820 < 0.01429` 이므로 **그래프 단독 노드는 어떤 경우에도 top-10 에 못 들어간다**.
코드 주석(707~714행)에 이 사실이 이미 정직하게 기록되어 있다.
진입 조건은 `graph_weight > (rrf_k+1)/(rrf_k+seed_k) ≈ 0.871` 인데,
그 값이면 이번엔 그래프가 검색을 압도해 정확도가 무너진다.
**하이퍼파라미터로 풀 문제가 아니다** — 설계 배치의 문제다(§6-④).

### 2.5 chunking 은 이미 부분적으로 옳다

`index.py` 의 색인 텍스트 구성은 **`소속 법규명 + 조제목 + 조내용`** 이다.
이는 §3.3 에서 소개할 **Summary-Augmented Chunking(SAC)** 의 경량판에 해당한다 —
조문 단위 chunk 에 문서 수준 식별정보를 주입해 "엉뚱한 문서에서 조문을 집어오는" 실패를 줄인다.
설계 방향은 옳다. 다만 주입되는 것이 **법규명 문자열 하나뿐**이라 개선 여지가 크다(§6-③).

또한 `retrieve.py` 는 원문 미러링 금지 규율에 따라 인덱스에 **메타데이터만** 저장하고
본문은 질의 시점에 SQLite 에서 `doc_key` 로 재조회한다. 신선도·용량 양립 측면에서 좋은 설계이고,
재랭킹 도입 시에도 그대로 유지된다(재랭커는 재조회된 본문을 받으면 된다).

### 2.6 반론 검토 — "가중치만 튜닝하면 되는 것 아닌가?"

정직하게 확인해야 할 반론이다. RRF 에서 dense 채널 가중치 `w` 를 쓸어보았다
(§4 의 28질의 평가셋, 조례 단위 top-5):

| dense weight `w` | Success@5 | P@5 | |
|---:|---:|---:|---|
| 0.0 | 0.6429 | 0.4714 | dense 완전 제거 = BM25 단독 |
| 0.1 | 0.6429 | 0.4857 | |
| **0.25** | **0.6786** | **0.4857** | **최적** |
| 0.5 | 0.6786 | 0.4714 | |
| 0.75 | 0.6786 | 0.4714 | |
| 1.0 | 0.6786 | 0.4500 | **현행 RRF** |
| 1.5 | 0.6429 | 0.4143 | |

**읽는 법 — 두 가지를 동시에 인정해야 한다.**

1. **현행 dense 가 100% 무해무익한 것은 아니다.** `w=1.0`(현행)이 BM25 단독보다
   Success@5 는 오히려 높다(0.6786 vs 0.6429, 28건 중 1건 차이).
   직전 라운드에서 하이브리드가 졌던 것과 결과가 다른데, 이유는 **평가셋이 다르기 때문**이다.
   §4 의 이번 평가셋은 질의 어휘를 조례명과 **일부러 어긋나게** 만들어
   dense 가 기여할 여지를 남겨 두었다. 반면 직전 라운드 질의는 어휘가 더 정렬되어 있어
   BM25 가 유리했다. **두 결과 모두 참이며, 서로 모순되지 않는다.**
2. **그러나 dense 는 정밀도를 갉아먹는다.** `w` 를 1.0 → 0.25 로 낮추면
   P@5 가 0.4500 → 0.4857 로 오른다. 즉 현행 가중치는 **잡음을 과대평가**하고 있다.

**결론: 튜닝으로 얻을 수 있는 최대치는 P@5 +0.0143, Success@5 +0.0357 다.**
§0 에서 본 재랭킹 여지 **+17.9pp** 와 비교하면 **한 자릿수 분의 일**이다.
가중치 튜닝은 **하는 편이 낫지만(즉시 적용 권장: `w=0.25`), 문제의 해법이 아니다.**
채널 자체를 바꿔야 한다.

### 2.7 토크나이저: 형태소분석기가 설치되어 있는데 안 쓰고 있다

`Tokenizer` 는 어절 토큰 + 어절 내부 char 2·3-gram 혼합이며,
docstring 에 "한국어 형태소분석기 부재 대응"이라고 적혀 있다.
그런데 **`kiwipiepy 0.23.0` 이 이미 설치되어 있다.**

한국어는 교착어라 `선생님께서` = `선생`+`님`+`께서` 처럼 조사·어미가 붙는다.
char n-gram 은 이 변이를 흡수하는 장점이 있지만, 동시에 무의미 조각을 대량 생성해 어휘를 부풀린다.
실측이 이를 보여준다 — **dense 어휘 655,934개 vs BM25 어휘 335,912개**,
포스팅은 21.6M vs 10.8M 로 **2배**다. 인덱스 용량과 질의 지연을 2배로 쓰면서
정확도는 더 낮은 채널을 유지하고 있는 셈이다.

---

## 3. 관련 연구 (원문 확인한 것만)

### 3.1 법률 RAG 검색 벤치마크

**LegalBench-RAG** — Pipitone & Houir Alami (2024), arXiv:2408.10343, 2024-08-19 제출.
법률 도메인에서 **검색 단계만을** 평가하는 최초의 벤치마크다. 기존 LegalBench 가 LLM 의 생성 능력을
평가한 반면 RAG 의 retrieval 컴포넌트 평가에는 공백이 있다는 문제의식에서 출발했다.
LegalBench 질의의 컨텍스트를 원문 위치로 역추적해 **6,858개 질의-정답 쌍 / 79M자 이상 코퍼스**를
법률 전문가가 전수 주석했다. 핵심 주장은 "문서 ID 나 큰 덩어리가 아니라 **최소한의 정확한 텍스트 조각**을
찾아야 한다"는 것 — 긴 컨텍스트는 비용·지연을 늘리고 LLM 이 잊거나 환각하게 만들기 때문이다.
경량판 LegalBench-RAG-mini 도 함께 제공한다.

> **우리 적용:** 우리는 이미 **조문 단위**로 검색하고 있다(문서 단위 = 조문 1개, `doc_key = oa_id | article_id`).
> LegalBench-RAG 가 요구하는 "최소 단위 정확 검색"과 정렬되어 있다. 이 점은 유지해야 한다.

**COLIEE** (Competition on Legal Information Extraction/Entailment) — 법령·판례 검색의 대표 국제 대회.
COLIEE 2025 개요는 ICAIL 2025 (ACM, doi:10.1145/3769126.3785016) 에 실렸다.
NOWJ 팀(Nguyen et al., 2025, arXiv:2509.08025, 2025-09-09 제출)은 Task 2 에서 **F1 0.3195 로 1위**를 했는데,
그 구성이 우리에게 시사적이다:

```
사전랭킹(BM25 / BERT / monoT5)  →  임베딩(BGE-m3, LLM2Vec)  →  LLM 재랭킹(Qwen-2, QwQ-32B, DeepSeek-V3)
```

즉 **최신 SOTA 도 BM25 를 버리지 않는다.** BM25 는 **1단 후보생성기**로 남고,
정확도는 **뒷단 재랭킹**에서 만든다. 대회 전반에서도 다단계 검색 파이프라인이 보편화되었고
BM25 는 제출작 절반에 등장했다.

### 3.2 "BM25 가 강하다"는 것은 우리만의 관측이 아니다

**Rosa, Rodrigues, Lotufo, Nogueira (2021), "Yes, BM25 is a Strong Baseline for Legal Case Retrieval"**
— arXiv:2105.05686, COLIEE 2021 Task 1.

제목 그대로다. 저자들은 **아무 튜닝도 하지 않은 vanilla BM25** 로 COLIEE 2021 Task 1 에서
**2위**를 했고 제출작 중앙값을 크게 상회했다고 보고한다.

이 논문은 우리 결과를 **정상**으로 만들어 준다. 법률 검색에서 BM25 가 신경망 방법을 이기는 것은
드문 일이 아니다. 법률 텍스트는 (i) 용어가 고도로 표준화되어 있고, (ii) 정답 문서가 질의의 핵심 용어를
**문자 그대로** 포함하는 경우가 많아 어휘 매칭에 극도로 유리하다.
따라서 우리가 할 일은 "BM25 를 버리는 것"이 아니라
**BM25 를 1단으로 확정하고 그 위에 무엇을 얹을지**를 정하는 것이다.

### 3.3 대규모 유사문서 코퍼스의 고유 실패: DRM

**Reuter, Lingenberg, Liepiņa, Lagioia, Lippi, Sartor, Passerini, Sayin (2025),
"Towards Reliable Retrieval in RAG Systems for Large Legal Datasets"**
— arXiv:2510.06999, 2025-10-08 제출, NLLP@EMNLP 2025 채택.

이 논문이 정의한 실패 모드 **Document-Level Retrieval Mismatch (DRM)** — 검색기가
**완전히 잘못된 원본 문서**에서 정보를 고르는 현상 — 은 우리 문제와 정확히 겹친다.
저자들은 이것이 **"구조적으로 유사한 문서가 대량으로 있는 DB"** 에서 특히 심각하다고 지적한다.

우리 코퍼스가 바로 그 극단이다. 전국 자치법규는 **같은 표준 조례안을 복제**해서 만들어진다.
본문이 확보된 4,921개 조례의 주제 분포 실측(지역명 제거 후 정규화):

| 건수 | 정규화된 조례명 |
|---:|---|
| 41 | 동물보호 조례 |
| 40 | 지방공무원 정원 규칙 |
| 27 | 장애인가정 출산지원금 지원 조례 |
| 25 | 반려동물 보호 및 학대방지 조례 |
| 25 | 지방공무원 정원 조례 |
| 23 | 동물보호 및 관리에 관한 조례 |
| 22 | 야생동물에 의한 피해보상 등에 관한 조례 |
| 21 | 기후위기 대응을 위한 탄소중립ㆍ녹색성장 기본 조례 |

지역명만 다르고 조문은 거의 동일한 문서가 **수십 개씩** 존재한다.
DRM 이 우리 시스템의 **주된 실패 모드**일 수밖에 없는 구조다.

처방으로 제시된 **Summary-Augmented Chunking (SAC)** 은 각 chunk 에 **문서 수준 합성 요약**을 덧붙여,
chunking 과정에서 소실되는 전역 맥락을 주입한다. 결과적으로 DRM 이 크게 감소하고 텍스트 수준
정밀도·재현율도 함께 올랐다. 흥미로운 부수 발견: **법률 전문지식을 반영한 요약보다 범용 요약이 더 좋았다.**
(→ 우리가 LLM 없이 규칙 기반 메타데이터로 근사해도 상당 부분 얻을 수 있다는 뜻. §6-③)

### 3.4 한국어 검색 임베딩과 리랭커

**KURE-v1** (Korea University Retrieval Embedding) — nlpai-lab, 2024-12-21 공개,
github.com/nlpai-lab/KURE. `BAAI/bge-m3` 를 CachedGISTEmbedLoss 로 파인튜닝한 한국어 특화 검색 임베딩이다.
1024차원, 시퀀스 길이 8192, 한국어 질의-문서 쌍 200만건(하드네거티브 5개/샘플),
배치 4096, lr 2e-05, 1 epoch 학습. 공개 리더보드(MTEB-ko-retrieval) 실측치:

| 모델 | NDCG@10 | Recall@10 |
|---|---|---|
| **KURE-v1** | **0.69473** | **0.79682** |
| BGE-m3-ko | 0.68748 | ~0.78–0.79 |
| BGE-m3 | 0.68723 | ~0.78–0.79 |
| Snowflake Arctic Embed | 0.68189 | ~0.78–0.79 |

평가셋 8종(Ko-StrategyQA, AutoRAG Retrieval, MIRACL, PublicHealthQA, Belebele, MrTidy,
MultiLongDoc, XPQA)에 **AutoRAG Retrieval 의 법률(legal) 도메인 서브셋**이 포함되어 있다는 점이
우리에게 중요하다.

> **주의 — 과장하지 말 것.** KURE-v1 의 우위는 bge-m3 대비 **+0.0075 NDCG@10 로 작다**.
> 즉 "한국어 특화"의 이득보다 **"진짜 신경망 임베딩을 쓴다"는 사실 자체의 이득**이
> 우리 경우 훨씬 클 것으로 예상된다 (우리 현재 baseline 은 신경망이 아예 아니므로).

한국어 금융 텍스트 사례연구(TWICE, arXiv:2502.07131)도 한국어 데이터로 파인튜닝된 모델
(kure-v1 포함)이 도메인 특화 과제에서 더 견고했다고 보고한다.

**한국어 토크나이징.** Elasticsearch 공식 한국어 플러그인 **Nori** 는 MeCab 계열 형태소분석기 기반이며,
Elastic 공식 문서는 한국어의 어절(eojeol)이 형태소의 교착 결합(`선생님께서` → `선생`+`님`+`께서`)이므로
형태소 단위 색인이 필요하다고 설명한다. 우리는 Nori 대신 **kiwipiepy**(설치 완료)를 쓸 수 있다.

### 3.5 융합(fusion)과 재랭킹 — 효과 크기

**RRF vs 가중합.** RRF 는 점수를 무시하고 순위만 쓰기 때문에, 스케일이 다른 채널
(BM25 는 상한 없음, 코사인은 [0,1])을 정규화 없이 합칠 수 있다는 장점이 있다.
OpenSearch 등 상용 검색엔진이 기본값으로 채택한 이유이며,
가중합 방식보다 튜닝이 덜 필요하고 점수 분포 변동에 강건하다고 보고된다.

> **그러나 RRF 의 전제는 "각 채널이 각자 유능하다"** 는 것이다.
> RRF 는 유능한 채널을 여럿 합칠 때 강하지, **무능한 채널의 잡음을 걸러주지 못한다** —
> 순위만 보므로 dense 의 1위가 아무리 엉터리여도 `1/61` 이라는 후한 점수를 받는다.
> 우리 §2.2 의 실패가 정확히 이것이다. **RRF 를 바꿀 게 아니라 채널을 고쳐야 한다.**

**cross-encoder 재랭킹의 효과 크기.** 질의와 문서를 **함께** 인코딩해 토큰 간 상호작용을 보는
cross-encoder 는 bi-encoder 대비 큰 폭의 이득을 낸다. 문헌에 보고된 범위:

- MS MARCO 에서 bi-encoder·late-interaction 대비 최대 **+10 nDCG points**
- SPLADE-v3 같은 강한 학습형 sparse retriever 대비 **+5~7 points**
- BEIR 평균에서 SOTA bi-encoder 대비 **+4 points 이상** (3B monoT5 기준)
- 모델 규모 확대는 in-domain 이득은 작지만 **out-of-domain(BEIR) 이득이 크다**
  → 학습 데이터가 없는 우리 도메인에 유리한 성질

**대안 아키텍처는 왜 지금 아닌가.** SPLADE(학습형 sparse), ColBERT(late interaction)도 후보다.
페이지 단위 검색 비교에서 ColBERT 가 BM25·SPLADE 를 모두 상회했다는 보고가 있다.
그러나 둘 다 **전체 코퍼스 재색인**이 필요하고, ColBERT 는 토큰마다 벡터를 저장해 인덱스가 수십 배로 커진다
(그래서 Token Pooling, PQ 압축 같은 후속 연구가 따로 있을 정도다).
현재 62,460 문서이고 본문 156,027건 전수 수집이 진행 중이라 코퍼스는 곧 **수십 배**가 된다.
CPU 만으로 운용하기엔 부담이 크다.

> **cross-encoder 재랭킹은 top-50 만 처리하므로 색인을 전혀 건드리지 않는다.**
> 코퍼스가 커져도 재랭킹 비용은 불변이다. 비용 대비 이득이 압도적이다.

### 3.6 신뢰성: 법률 RAG 는 여전히 환각한다

**Magesh, Surani, Dahl, Suzgun, Manning, Ho (2024/2025), "Hallucination-Free? Assessing the
Reliability of Leading AI Legal Research Tools"** — arXiv:2405.20362;
*Journal of Empirical Legal Studies*, 2025, doi:10.1111/jels.12413; Stanford RegLab · HAI.

상용 법률 AI(Lexis+ AI, Westlaw AI-Assisted Research, Ask Practical Law AI)를 **202개 법률 질의**로
사전등록(preregistered) 평가한 최초의 실증 연구다. 결과: 세 도구 모두 **17~33% 비율로 환각**했다.
범용 챗봇(GPT-4)보다는 개선되었지만, LexisNexis 의 "100% hallucination-free linked legal citations"
같은 벤더 주장에 대해 저자들은 과장되었다고 결론지었다.

> **우리 설계에 주는 함의.** 우리 시스템은 조문 단위로 검색하고 모든 히트에
> `official_url`(law.go.kr 원문 링크)과 `content_hash` 를 함께 반환한다(히트 구조 실측 확인).
> 이것은 **citation-grounded** 설계로, 위 연구가 지적한 실패를 구조적으로 줄인다.
> 지자체 실무자는 조례를 **근거로** 문서를 쓰기 때문에,
> **"틀린 조문을 그럴듯하게 요약하는 것"이 최악**이다.
> 검색 정확도 개선은 UX 문제가 아니라 **신뢰성 문제**다. (심사 배점 '정확성 15' 와 직결)

### 3.7 GraphRAG 후속 연구 — 그래프가 무익했던 사례는 이미 많이 보고되었다

우리의 "그래프 기여 0" 관측은 고립된 사고가 아니다.

**Han et al. (2025), "RAG vs. GraphRAG: A Systematic Evaluation and Key Insights"**
— arXiv:2502.11371, 2025-02-17 제출(최신 개정 2026-03-04).
QA 와 질의기반 요약에 걸쳐 두 패러다임을 통합 프로토콜로 비교하고,
**실패 모드·효율 트레이드오프·평가 편향**을 분석한다.

**GraphRAG-Bench** — "When to use Graphs in RAG: A Comprehensive Analysis for Graph
Retrieval-Augmented Generation", arXiv:2506.05690, 2025-06 제출, **ICLR 2026 채택**,
github.com/GraphRAG-Bench/GraphRAG-Benchmark. 초록이 문제의식을 그대로 적고 있다:
최근 연구들이 GraphRAG 가 많은 실세계 과제에서 vanilla RAG 에 **자주 못 미친다**고 보고하고 있으며,
따라서 **"그래프 구조가 측정 가능한 이득을 주는 시나리오가 어디인가"** 를 물어야 한다는 것이다.
벤치마크는 사실검색 → 복합추론 → 맥락요약 → 창의생성으로 난이도를 올려가며 구성되었고,
그래프 구축부터 검색·생성까지 파이프라인 전체를 평가한다.

**문헌이 수렴하는 조건부 결론:**

| 질의 유형 | 그래프 효과 |
|---|---|
| **단일 홉 사실 조회** (local / lookup) | **손해**. 단일홉 QA 정확도 하락 보고(Natural Questions −13.4%, 시간민감 질의 −16.6%) |
| **다중 홉 관계 추론** (multi-hop) | **이득**. 관계 추론이 필요한 질의에서 강점 |
| **전역 요약** (global / community) | 혼재. 커뮤니티 기반 global search 는 여러 데이터셋에서 vanilla RAG 미달 보고 |

원인도 일관되게 지목된다: 그래프 확장은 **recall 을 넓히는 대신 잡음을 대량 주입**한다.
보고된 context relevance 는 GraphRAG 계열 **36.86~54.61%** 로 vanilla RAG **62.87%** 보다 **낮았다**.

> **우리에게의 결론.** 우리 주 질의는 **"이런 조례 있어?"라는 단일 홉 조회**다.
> 문헌이 말하는 **"그래프가 손해 보는 구간"에 정확히 해당**한다.
> 반면 우리 그래프(위임관계 3,203 · 인용 5,377 · 인접지역 1,098 · 승계 17)가 답할 수 있는
> **"이 조례의 상위법은?", "인접 지자체는 뭘 만들었나?", "이 조례를 인용한 조례는?"** 은
> 전형적인 **다중 홉 관계 질의**다.
> 즉 그래프를 버릴 게 아니라 **쓰는 자리를 옮겨야 한다**(§6-④).
> 그리고 이 재배치는 우리 타깃 사용자의 실제 과업
> **"우리와 비슷한 지역이 이미 만든 조례 찾기"** 와 오히려 더 잘 맞는다.

---

## 4. 평가 설계 — 왜 지표를 바꿨는가

직전 라운드의 MRR 은 4방식 모두 1.000 으로 **포화**해 아무것도 구분하지 못했다.
Recall@5 도 문제가 있다 — 우리 코퍼스는 같은 주제 조례가 수십 개씩 있어(§3.3) 정답 집합이 크고,
그러면 Recall@5 의 상한 자체가 낮아져 해석이 어렵다.

그래서 이번 라운드는 **조례(문서) 단위**로 다음 두 지표를 쓴다:

- **Success@k** — top-k 안에 정답 주제 조례가 **하나라도** 있으면 1. 사용자 관점의 "찾았다/못 찾았다".
- **P@k** — top-k 중 정답 주제인 비율. 목록 품질(잡음 비율).

**평가셋(28질의, 수작업).** 실무자가 실제로 칠 법한 **자연어**로 질의를 쓰고,
공식 조례명과 **어휘가 의도적으로 겹치지 않게** 만들었다.
정답은 조례명에 대한 정규식으로 정의한다. 예:

| 질의 | 정답 정의(정규식) |
|---|---|
| 개 물림 사고 막으려면 무슨 규정이 있나 | `동물보호\|반려동물.*(학대\|보호)\|맹견` |
| 멧돼지가 농작물 망쳤을 때 보상 | `야생동물\|유해야생` |
| 탄소 배출 줄이기 위한 지자체 계획 | `탄소중립\|기후위기\|녹색성장\|온실가스` |
| 아이 키우기 힘든 임산부 몰래 낳는 제도 | `보호출산\|위기\s*임신` |
| 동네 가게 사장님 대출 도와주는 제도 | `소상공인\|중소기업.*육성\|전통시장` |

이렇게 하면 **어휘 매칭만으로는 풀리지 않는** 질의가 섞여, 의미 검색의 기여를 실제로 측정할 수 있다.
(전체 28건은 재현 스크립트에 포함)

---

## 5. 실험 결과

### 5.1 실험 설계

모든 실험은 **현재 인덱스(62,460 문서, 2026-08-19 빌드)를 그대로 두고** 수행했다.
전량 재임베딩은 CPU 로는 수 시간이 걸려 이번 라운드에 포함하지 못했고,
대신 **후보 재랭킹 형태**로 신경망 모델의 기여를 측정했다.
이는 §6-② 가 제안하는 실제 배치 형태와 동일하므로, 측정값이 곧 구현 가능한 값이다.

| 시스템 | 구성 |
|---|---|
| BM25 | 현행 BM25 단독 (`k1=1.2, b=0.75`) |
| Dense(char-ngram-TF) | 현행 dense 채널 단독 |
| Hybrid RRF | 현행 기본값 (`rrf_k=60`, 두 채널 동일 가중) |
| Hybrid weighted | min-max 정규화 후 `alpha=0.5` |
| BM25→KURE-v1 재랭킹 | BM25 top-50 → KURE-v1 bi-encoder 코사인 재정렬 |
| BM25→CrossEncoder 재랭킹 | BM25 top-50 → `bge-reranker-v2-m3` 재정렬 |
| BM25+Dense 합집합→CrossEncoder | 두 채널 합집합 top-50 → `bge-reranker-v2-m3` 재정렬 |

재랭킹 입력 텍스트는 `조례명 + 조제목 + 본문(최대 900자)` 이며, 본문은 SQLite 에서 재조회했다
(§2.5 의 무미러링 설계를 그대로 사용). 지표는 §4 의 Success@k / P@k, 조례(문서) 단위 중복 제거 후 top-5.

### 5.2 결과

28질의 · 조례 단위 top-5 · 전부 실제 실행 결과다.

| 시스템 | P@5 | Success@5 | P@1 |
|---|---:|---:|---:|
| Dense (char-ngram-TF, 현행) | 0.3071 | 0.5714 | 0.3214 |
| Hybrid RRF (현행 기본값) | 0.4571 | 0.6786 | 0.5000 |
| Hybrid weighted (α=0.5) | 0.4571 | 0.6786 | 0.5000 |
| **BM25 (기준선)** | **0.4714** | **0.6429** | **0.5357** |
| BM25→KURE-v1 재랭킹 (top-50) | 0.5143 | 0.7857 | 0.5000 |
| **BM25→CrossEncoder 재랭킹 (top-50)** | **0.6643** | **0.8214** | **0.7143** |
| BM25+Dense 합집합→CrossEncoder | 0.6500 | 0.8214 | 0.7143 |

**BM25 대비 개선폭:**

| 지표 | BM25 | +CrossEncoder | 절대 | 상대 |
|---|---:|---:|---:|---:|
| P@5 | 0.4714 | 0.6643 | **+0.1929** | **+40.9%** |
| Success@5 | 0.6429 | 0.8214 | **+0.1785** | **+27.8%** |
| P@1 | 0.5357 | 0.7143 | **+0.1786** | **+33.3%** |

### 5.3 결과 해석 — 다섯 가지

**(1) cross-encoder 재랭킹이 이론적 상한을 100% 회수했다.**
§6 에서 측정한 후보군 천장은 **Success@50 = 0.8214** 였다.
cross-encoder 재랭킹의 Success@5 는 **정확히 0.8214** 다.
즉 **top-50 안에 있던 정답을 하나도 빠짐없이 top-5 로 끌어올렸다.**
재랭킹 측면에서 이보다 더 잘할 수는 없다. 남은 개선은 전부 **재현율 문제**다.

**(2) 현행 dense 는 단독으로 명백히 열등하다.** P@5 0.3071 은 BM25 0.4714 의 **65%** 수준이고,
P@1 은 0.3214 대 0.5357 로 **60%** 다. §2 의 진단이 지표로 확인되었다.

**(3) 하이브리드는 정밀도를 떨어뜨린다.** Hybrid RRF 의 P@5 0.4571 < BM25 0.4714,
P@1 0.5000 < 0.5357. Success@5 만 0.6786 로 소폭 높다(§2.6 의 설명과 일치).
**RRF 든 weighted 든 결과가 동일**한 것도 주목할 만하다 — 융합 방식이 문제가 아니라
**섞는 재료가 문제**라는 뜻이다.

**(4) 잡음 채널은 후보 생성에도 도움이 안 된다.**
`BM25+Dense 합집합→CrossEncoder` (P@5 0.6500) 가 `BM25→CrossEncoder` (0.6643) **보다 낮다.**
"dense 가 정밀도는 나빠도 recall 은 넓혀주니 후보 풀에는 넣자"는 통념이
**우리 데이터에서는 반증되었다.** 강력한 재랭커를 앞에 두어도, 잡음 후보는 순이익이 음수다.

**(5) bi-encoder(KURE)보다 cross-encoder 가 확실히 낫다.**
KURE 재랭킹 P@5 0.5143 vs cross-encoder 0.6643. §3.5 문헌의 효과 크기 서열과 일치한다.
다만 KURE 도 BM25 대비 Success@5 를 0.6429 → 0.7857 (**+14.3pp**) 올렸다 —
**진짜 신경망 표현이 들어오면 그 자체로 큰 이득**이라는 §3.4 의 예상이 맞았다.
KURE 의 P@1 이 0.5000 으로 BM25(0.5357)보다 낮은 것은,
bi-encoder 가 주제는 맞히되 **최상위 1건을 고르는 변별력**은 약하다는 뜻이다.

### 5.4 비용 — 정직하게

CPU 전용 환경의 실측 소요(28질의 기준):

| 단계 | 소요 | 질의당 |
|---|---:|---:|
| BM25 + dense 검색 (62,460 문서) | 0.5s | **0.018s** |
| KURE-v1 bi-encoder 재랭킹 (50건) | 294.7s | 10.5s |
| CrossEncoder 재랭킹 (50건) | 315.4s | **11.3s** |
| 모델 로드 (1회) | KURE 6.3s / CE 4.8s | – |

**질의당 11.3초는 대화형 서비스에 그대로 쓸 수 없다.** 완화 방안:

- top-50 → **top-20** 으로 축소 (소요 약 4.5s, headroom 은 Success@20 = 0.8214 로 **동일** → **손실 없음**)
- 더 작은 리랭커 사용, 또는 ONNX/양자화
- 결과 캐시(같은 질의 반복이 많은 업무 특성상 효과적)
- GPU 확보 시 10~50배 단축

> **중요:** Success@20 과 Success@50 이 **둘 다 0.8214** 라는 §6 의 측정 덕분에,
> **top-20 으로 줄여도 정확도 손실이 없다**고 말할 수 있다. 이것이 이번 라운드에서
> 가장 실무적으로 유용한 발견이다.

### 5.5 재현 방법

```
평가셋      : 28질의 수작업 라벨 (질의 자연어 + 정답 조례명 정규식)
인덱스      : F:/policy_maps/system/data/index/all  (n_docs=62460, built 2026-08-19)
DB          : F:/policy_maps/system/data/policymap.db  (읽기전용 URI 접속)
환경변수    : PYTHONIOENCODING=utf-8
모델 캐시   : ~/.cache/huggingface  (KURE-v1 · bge-m3 · bge-reranker-v2-m3 등 다운로드 완료)
```

측정 스크립트는 인덱스와 DB를 **읽기 전용으로만** 접근한다
(본문 전수 수집 배치가 동시 실행 중이므로 쓰기 트랜잭션 없음).

**평가셋 전문(28질의).** 개선안 ⑤에 따라 `tests/` 로 옮길 때 그대로 쓰면 된다.

```python
# query = 실무자 자연어 질의(공식 조례명과 어휘가 의도적으로 다름)
# gold  = ordinances.name 에 대한 정규식. 정답 문서 = 해당 조례에 속한 모든 조문.
EVAL = [
 ("길고양이 밥 주는 곳 설치 근거",            r"길고양이|고양이"),
 ("개 물림 사고 막으려면 무슨 규정이 있나",     r"동물보호|반려동물.*(학대|보호)|맹견"),
 ("유기견 안락사 관련 규정",                 r"동물보호|유기동물|동물.*관리"),
 ("멧돼지가 농작물 망쳤을 때 보상",           r"야생동물|유해야생"),
 ("아기 낳으면 주는 지원금",                 r"출산|출생|산모|모자보건"),
 ("장애인 부모가 출산할 때 받는 돈",          r"장애인가정\s*출산"),
 ("탄소 배출 줄이기 위한 지자체 계획",         r"탄소중립|기후위기|녹색성장|온실가스"),
 ("동네 가게 사장님 대출 도와주는 제도",       r"소상공인|중소기업.*육성|전통시장"),
 ("차 댈 곳 만들고 관리하는 규정",            r"주차장"),
 ("건물 지을 때 지켜야 하는 지자체 규정",      r"건축"),
 ("공무원 출장비 얼마 주나",                 r"여비"),
 ("시청 직원 몇 명까지 뽑을 수 있나",         r"정원"),
 ("남녀 차별 없애기 위한 기본 규정",          r"양성평등|성평등|여성"),
 ("재난 났을 때 대응 조직",                 r"재난|안전대책본부|통합방위|민방위"),
 ("나라 땅이나 건물 빌려쓰는 규정",           r"공유재산|국유재산"),
 ("반려견이랑 같이 여행 가는 거 지원",        r"반려동물.*동반|반려견.*순찰|반려동물산업"),
 ("아이 키우기 힘든 임산부 몰래 낳는 제도",    r"보호출산|위기\s*임신"),
 ("세금 제대로 냈는지 조사하는 절차",         r"세무조사|지방세"),
 ("결재를 누가 대신 하는지 정한 규정",        r"전결|사무위임"),
 ("돈 모아두는 기금 운용 규정",              r"기금"),
 ("상 주는 규정",                          r"포상|표창"),
 ("공무원 인사 관련 세부 규칙",              r"인사\s*규칙|임용"),
 ("도서관 짓고 운영하는 규정",               r"도서관"),
 ("어르신 복지 지원",                       r"노인|고령|경로"),
 ("청년들 정착 지원",                       r"청년"),
 ("학교 밥값 지원",                         r"급식"),
 ("상수도 요금 규정",                       r"수도|급수|상수도"),
 ("주민이 예산 짜는 데 참여",                r"주민참여예산|주민참여"),
]
```

**평가셋의 한계(명시).** 정답을 조례명 정규식으로 근사했으므로,
조례명에는 안 드러나지만 내용상 관련 있는 조문은 오답으로 처리된다(**정답률을 과소평가**).
반대로 이름만 맞고 내용이 다른 조례는 정답으로 처리된다(**과대평가**).
28건은 표본이 작아 1건 차이가 0.0357 로 움직인다 — **소수점 셋째 자리는 신뢰하지 말 것.**
그럼에도 §5.2 의 cross-encoder 개선폭(+0.19)은 **5건 이상의 차이**라 표본 잡음으로 설명되지 않는다.

---

## 6. 개선안 — 우선순위표

먼저 **개선 여지가 어디에 있는지**를 분해한다. BM25 단독의 성공률을 k 별로 측정했다(28질의):

| k | Success@k | P@k |
|---:|---:|---:|
| 5 | 0.6429 | 0.4714 |
| 10 | 0.7500 | 0.4357 |
| 20 | **0.8214** | 0.4196 |
| 50 | **0.8214** | 0.3114 |

이 표가 개선안 전체의 근거다.

- **Success@5 0.6429 → Success@20 0.8214**: 정답이 후보군에는 있는데 **5위 밖에 있다**.
  **+17.9pp 는 재랭킹만으로 회수 가능**하다. → 개선안 ②
- **Success@20 = Success@50 = 0.8214**: k 를 20에서 50으로 늘려도 안 오른다.
  나머지 **17.9pp(28질의 중 5건)는 top-50 후보군에 아예 없다.**
  재랭킹으로는 절대 못 고친다. **재현율 자체를 올려야** 한다. → 개선안 ①③
- **P@50 0.3114 로 하락**: 후보를 늘릴수록 잡음이 는다. 무작정 k 를 키우는 것은 답이 아니다.

즉 **① 재현율(진짜 dense) + ② 정밀도(재랭킹)** 두 축을 모두 손대야 하고,
그 둘은 서로 다른 결함을 고치므로 **가산적**이다.

### 우선순위표

| 순위 | 개선안 | 무엇을 고치는가 | 측정된 효과 | 구현 비용 | 위험 |
|---:|---|---|---|---|---|
| **①** | **dense 채널을 KURE-v1 로 교체** | 재현율. top-50 **밖** 정답(17.9pp) | KURE 재랭킹만으로 Success@5 **+14.3pp** (§5.2). 전량 교체 효과는 **미측정** | 중 (전량 재임베딩) | 중 (CPU 시간) |
| **②** | **cross-encoder 재랭킹 (top-20~50)** | 정밀도. 후보군 **안** 정답(17.9pp) | **P@5 +40.9%, Success@5 +27.8%, P@1 +33.3%** — 천장 100% 회수 (§5.2) | **소** (색인 불변) | 저 (지연 11.3s/질의) |
| **③** | **kiwi 형태소 + 문서맥락 주입(SAC 경량판)** | 재현율 + DRM | 어휘 87.7% 축소 실측 (§2.7). 정확도 효과는 **미측정** | 중 (재색인) | 저 |
| **④** | **그래프를 랭킹에서 분리 → "관련 조례" UI** | 잡음 제거 + 신규 가치 | 현재 기여 0 (16/16 동일). 분리 시 손실 없음 | 소 | 매우 저 |
| ⑤ | 평가 상시화 (회귀 테스트) | 재발 방지 | – | 소 | – |
| 즉시 | RRF dense 가중치 1.0 → **0.25** | 잡음 억제 | P@5 +0.0143 (§2.6) | **1줄** | 없음 |

> **정직성 표기:** ②는 **실측 검증되었다**. ①③은 **진단은 실측, 효과는 부분 측정 또는 미측정**이다.
> ①의 "전량 dense 인덱스" 효과와 ③의 정확도 효과는 다음 라운드 측정 대상이다.

**배포 순서 권고:** ②(오늘) → 즉시(1줄) → ④(UI 분리) → ③(재색인) → ①(재임베딩) → ⑤(상시화).

---

### ① dense 채널을 KURE-v1 로 교체 — *가장 근본적*

**무엇을.** `Embedder(model_name='sbert:nlpai-lab/KURE-v1')` 경로는 **이미 코드에 있다**
(`policymap/parsers/embedding.py:_try_load_sbert`). 인덱스 빌드 시 이 model_name 을 넘기면
`index.py` 가 `dense_kind='dense'`, `dense_dim=1024` 로 행렬 인덱스를 만든다
(`_dense_matrix_search` 경로도 이미 구현되어 있다). **새 코드를 거의 안 써도 된다.**

**왜 KURE-v1 인가.** §3.4 — 한국어 검색 특화, MTEB-ko-retrieval NDCG@10 0.69473 로 1위,
평가셋에 법률 도메인 포함. bge-m3 기반이라 8192 토큰까지 받아 **긴 조문을 자르지 않는다**.
다만 bge-m3 대비 우위는 +0.0075 로 작으므로, **bge-m3 로 시작해도 무방**하다(로드가 4배 빠름).

**비용(정직하게).** CPU 전용이다. 62,460 문서 × 1024차원 인코딩은 수 시간,
본문 전수 수집이 끝나 코퍼스가 수십 배가 되면 **CPU 로는 현실적이지 않다.**
따라서 실제 채택 시 아래 중 하나가 필요하다:

- (a) 조문 전체가 아니라 **조례명+조제목만** 임베딩(짧아서 10배 이상 빠름) — 우리 질의는 대부분 주제 조회이므로 손실이 작을 것
- (b) `dense.mat.bin` 을 float32 대신 **int8 양자화**(용량 1/4)
- (c) GPU 확보

**부분 검증됨.** §5.2 에서 KURE-v1 을 **재랭커로만** 써도
Success@5 가 0.6429 → **0.7857 (+14.3pp)** 올랐다. 진짜 신경망 표현의 이득이 실재한다는 증거다.
다만 P@1 은 0.5000 으로 BM25(0.5357)보다 낮았다 —
bi-encoder 는 주제는 맞히되 **최상위 1건의 변별력**이 약하다.

> **주의 — 전량 dense 인덱스 효과는 아직 미측정이다.** §5 의 실험은
> 전량 재임베딩 대신 **BM25 후보를 KURE 로 재랭킹**해 측정한 것이다.
> 전량 dense 인덱스는 **top-50 밖의 정답까지 찾아낼 수 있다**는 점에서
> 재랭킹보다 이득이 클 수 있으나, 그것은 **가설이지 측정값이 아니다.**
> 다음 라운드에서 검증해야 한다.

**부수 효과 — 결함은 검색기 밖으로도 번져 있다(코드 확인).**

- `policymap/neural/gnn.py:122` — GNN 의 **노드 초기 피처**가 `_pemb.Embedder()` 기본값,
  즉 **같은 char n-gram TF** 다. 피처 블록 구성은
  `[라벨 원핫 | 텍스트해시 | 카테고리 원핫 | 수치]` 인데 이 중 **텍스트해시 블록 전체**가
  IDF 없는 n-gram TF 를 부호해싱으로 투영한 것이다.
  직전 라운드의 held-out AUC 0.7814 는 **이 열등한 입력 피처 위에서 얻은 값**이므로,
  ①을 적용하면 **GNN 성능도 함께 오를 가능성**이 있다(무료 개선 후보).
- `policymap/mcp_server/server.py:1335,2039` — 유사조례 폴백 경로가
  `similarity_edges(char-ngram-tf)` 로 표기되어 사용자에게 노출된다.

즉 ①은 검색기 하나가 아니라 **텍스트 표현 계층 전체**를 교체하는 작업이다.

---

### ② cross-encoder 재랭킹 — *가성비 1위, 지금 당장 가능*

**무엇을.** BM25 로 top-50 을 뽑고, `BAAI/bge-reranker-v2-m3` 로 (질의, 조문) 쌍을 채점해 재정렬한다.
`retrieve.py` 에 `rerank(conn, query, hits, k)` 함수를 추가하고 `answer_context()` 에서 호출하면 된다.

**왜 최우선 실행인가.**

- **색인을 전혀 건드리지 않는다.** 코퍼스가 156,027건으로 커져도 **재랭킹 비용은 불변**이다(top-50 고정).
- ①이 몇 시간~며칠 걸리는 동안에도 **오늘 붙일 수 있다.**
- 문헌상 효과 크기가 가장 크다(§3.5: BEIR +4점 이상, sparse 대비 +5~7점).
- **out-of-domain 이득이 크다**는 성질이 우리처럼 학습 데이터 없는 도메인에 유리하다.
- §2.2 의 실패 사례가 실제로 고쳐지는 것을 확인했다 — 재랭커는
  `산사태취약지역 조례`에 0.198, `유해야생동물 조례`에 0.014 를 주어 **14배로 분리**한다.
- 그리고 **우리 데이터에서 실측 검증되었다**(§5.2):
  **P@5 0.4714 → 0.6643 (+40.9%), Success@5 0.6429 → 0.8214 (+27.8%), P@1 0.5357 → 0.7143 (+33.3%).**
  후보군 천장(Success@50 = 0.8214)을 **100% 회수**했다.

**후보 풀은 BM25 만 쓸 것.** §5.2 (4) 에서 확인했듯
`BM25+Dense 합집합→CE`(P@5 0.6500) 가 `BM25→CE`(0.6643) **보다 나쁘다.**
①이 완료되어 dense 가 진짜 신경망이 되기 전까지는 **후보 생성에 dense 를 넣지 말라.**

**비용.** CPU 재랭킹은 느리다 — **질의당 11.3초**(top-50, §5.4).
`Success@20 = Success@50 = 0.8214` 이므로 **top-20 으로 줄이면 정확도 손실 없이 약 4.5초**가 된다.
그래도 느리면 더 작은 리랭커·ONNX 양자화·결과 캐시를 쓰거나,
**"정밀검색" 토글**로 노출해 사용자가 선택하게 한다.

**구현 위치.** `retrieve.py` 에 `rerank(conn, query, hits, k, *, model=...)` 를 추가하고
`answer_context()` 에서 호출한다. 인덱스·DB 스키마는 **전혀 건드리지 않는다.**
모델 로드는 1회 4.8초이므로 프로세스 수명 동안 캐시하면 된다.

---

### ③ kiwi 형태소 토크나이저 + 문서맥락 주입

**(3-a) 형태소 토크나이저.** `kiwipiepy 0.23.0` 이 **이미 설치되어 있다**. 실측 비교(조문 2,000건):

| 토크나이저 | 어휘 크기 | 소요 |
|---|---:|---:|
| 현행 어절+char 2·3-gram | 21,599 | 0.1s |
| kiwi 형태소(N/V/S/X/M) | **2,650** | 9.7s |

**어휘 87.7% 축소.** 그리고 결정적으로, 의미가 보존된다:

```
질의: "산사태 예방 및 복구에 관한 사항"

현행 ngram: ['관한','관한','구에','및','복구','복구에','복구에',
             '사태','사항','사항','산사','산사태','산사태','예방']
kiwi      : [('산사태',NNG),('예방',NNG),('및',MAG),('복구',NNG),
             ('에',JKB),('관하',VV),('ᆫ',ETM),('사항',NNG)]
```

현행 방식은 `산사태` 를 `산사` + `사태` 로 쪼갠다. **`사태`는 전혀 다른 뜻의 단어**이고,
이것이 §2.2 에서 본 오검색의 구체적 기제다. kiwi 는 `산사태` 를 하나의 명사로 유지한다.
또 `구에`, `복구에`, `관한` 같은 조사 결합 쓰레기 토큰이 사라진다.

kiwi 는 100배 느리지만(2,000건 9.7s → 156,000건 약 13분) **색인 시 1회 비용**이고,
질의 시에는 1건이라 무시할 수 있다.

> **권장 구성:** 형태소 토큰을 **주 자질**로 쓰고, char n-gram 은 **보조**로 남긴다
> (미등록어·오탈자 대응). 완전 교체보다 안전하다.

**(3-b) 문서맥락 주입(SAC 경량판).** 현재 색인 텍스트는 `법규명 + 조제목 + 조내용` 이다(§2.5).
DRM(§3.3)이 우리 주 실패 모드이므로 여기에 구조화 메타데이터를 더 넣는다:

```
[지자체명] [광역/기초] [소관부서] [분야(category_field)] [상위 위임법령명]
+ 법규명 + 조제목 + 조내용
```

`ordinances` 테이블에 `region_id`, `org_name`, `local_tier`, `department`,
`category_field` 가 이미 있고, `delegations` 3,203건으로 상위법령도 붙는다.
**LLM 요약 없이 규칙만으로** SAC 효과의 상당 부분을 얻을 수 있다 —
Reuter et al. 이 "전문지식 요약보다 범용 요약이 나았다"고 보고한 것과 같은 맥락이다.

이는 **"인접/유사 지자체의 같은 주제 조례 찾기"** 라는 우리 핵심 과업에 직접 기여한다.

---

### ④ 그래프는 검색 랭킹에서 빼고 "관련 조례 확장 제안" UI 로 재배치

**진단 요약.** 그래프는 (i) RRF 산식상 top-k 진입이 **수학적으로 불가능**하고(§2.4),
(ii) 문헌상 우리 같은 **단일 홉 조회 질의에서는 오히려 손해**다(§3.7).
현재 `hybrid_graph_search` 는 **비용만 쓰고 결과를 바꾸지 않는다**(16/16 동일).

**처방 — 버리지 말고 옮긴다.** 검색 결과 랭킹에서 그래프를 제거하고,
**검색이 끝난 뒤 별도 패널**로 노출한다:

```
[검색 결과]  "반려동물 등록 지원"
  1. 화천군 반려동물 복지 및 반려문화 조성 조례  §5 …
  2. 시흥시 반려동물 입양 활성화에 관한 조례      §3 …

──────────────────────────────────────────────
[이 조례와 연결된 것들]              ← 그래프의 자리
  ▸ 상위 위임법령    동물보호법 제15조        (delegations)
  ▸ 이 조례를 인용    3개 조례                (instrument_relations)
  ▸ 인접 지자체 유사  양구군·인제군·철원군      (region_adjacency)
  ▸ 연계 예산         2026 반려동물복지 12.4억  (ordinance_budget_link)
```

**왜 이게 더 나은가.**

1. **정확도를 해치지 않는다.** 랭킹에 섞이지 않으므로 잡음 주입(§3.7의 context relevance 하락)이 없다.
2. **그래프가 잘하는 질의만 준다.** "상위법은?", "누가 인용했나?", "이웃은 뭘 만들었나?" 는
   전형적 **다중 홉 관계 질의**로, 문헌이 말하는 그래프의 강점 구간이다.
3. **설명 가능하다.** `graph_expand` 는 이미 `path_str`(관계 경로)을 반환한다.
   "왜 이게 관련 있는지"를 근거와 함께 보여줄 수 있다 — 심사 배점 **정확성·전달성**에 유리.
4. **타깃 사용자 과업과 정확히 맞는다.** "우리와 비슷한 지역이 이미 만든 조례 찾기" 는
   검색이 아니라 **관계 탐색**이다. 그래프를 여기에 두면 제품의 차별점이 된다.
5. **평가가 정직해진다.** 검색 지표와 확장 지표를 분리 측정하면
   "그래프가 기여 0" 같은 착시가 다시 생기지 않는다.

> 즉 직전 라운드의 반증은 "그래프가 쓸모없다"가 아니라
> **"그래프를 잘못된 자리에 두었다"** 는 뜻이었다.

**[14. 법령그래프 표준 및 온톨로지](14_법령그래프_표준_및_온톨로지.md) 와의 접점.**
문서 14 가 지적하듯 **폐지된 조례를 추천하면 정확성 점수는 그 자리에서 무너진다.**
이 패널은 그래프 관계를 노출하는 자리이므로, `ordinances.repealed_on` / `status` /
`succession_status` 로 **반드시 필터링**해야 한다. 관계 술어(`amends`/`repeals`/`cites`)를
ELI·Akoma Ntoso 어휘에 정렬하면 이 패널이 곧 표준 준수의 가시적 증거가 된다.

---

### ⑤ 평가를 상시화한다

이번 라운드의 진짜 교훈은 **"측정하지 않으면 char n-gram 을 dense 라고 부르며 몇 달을 보낸다"** 는 것이다.

- 28질의 평가셋을 `tests/` 에 고정하고 `pytest` 회귀 테스트로 편입
- **Success@5 / P@5 를 릴리스 게이트**로 사용(현재 pytest 56 passed 에 추가)
- MRR 은 포화되어 무용하므로 **제외**
- 채널별(BM25/dense/rerank) 기여도를 매 빌드마다 기록 → 이번 같은 열화 복제본을 즉시 탐지



---

## 7. 예상 효과와 심사 배점 연결

### 7.1 정확도 로드맵

| 단계 | Success@5 | P@5 | 근거 |
|---|---:|---:|---|
| 현행 (Hybrid RRF) | 0.6786 | 0.4571 | **실측** |
| BM25 단독으로 되돌림 | 0.6429 | 0.4714 | **실측** |
| + dense 가중치 0.25 | 0.6786 | 0.4857 | **실측** |
| **+ cross-encoder 재랭킹 (②)** | **0.8214** | **0.6643** | **실측** |
| + 재현율 개선 (①③) | 0.8214 초과 | – | **가설 — 미측정** |

②만으로 **현행 대비 Success@5 +21.0%, P@5 +45.3%** 다.
①③의 목표는 Success@50 천장 자체를 0.8214 위로 올리는 것이다
(현재 28질의 중 5건은 top-50 안에 정답이 아예 없다).

### 7.2 심사 배점과의 연결

1차 심사 **독창성15 · 혁신성15 · 정확성15 · 데이터다양성15 · 데이터수급성20 · 완성도20** /
2차 **전달성15 · 효과성15 · 지속성15 · 활용성15 · 현실성20 · 파급성20**

| 배점 항목 | 이번 라운드가 기여하는 방식 |
|---|---|
| **정확성 15** | 가장 직접적이다. P@1 0.5357 → 0.7143. 게다가 **자기 시스템의 결함을 스스로 찾아내 수치로 반증하고 고친 기록**은 정확성 주장에 대한 최고의 증거다 |
| **완성도 20** | 진단 → 문헌 조사 → 실험 → 처방 → 재현 절차가 한 문서에 닫혀 있다. 회귀 테스트(⑤)로 상시화 |
| **혁신성 15** | ④ "그래프를 랭킹이 아니라 관계 탐색 패널로" 는 GraphRAG-Bench(ICLR'26)의 조건부 결론을 **우리 도메인에 맞게 재해석**한 설계 판단이다 |
| **현실성 20** | 비용을 숨기지 않았다 — 질의당 11.3초, GPU 없음, top-20 축소로 완화. **작동하는 조건을 명시한 제안**이 현실성이다 |
| **전달성 15** | 그래프 패널이 `path`(관계 경로)를 함께 보여줘 "왜 관련 있는지"를 설명한다 |
| **활용성 15 · 효과성 15** | 타깃 과업 "우리와 비슷한 지역이 이미 만든 조례 찾기" 에서 5건 중 3.3건이 정답(P@5 0.6643). 실무자가 목록을 훑는 비용이 실질적으로 줄어든다 |
| **지속성 15** | ②는 색인을 건드리지 않아, 본문 156,027건 수집이 끝나 코퍼스가 수십 배가 되어도 **재랭킹 비용은 불변**이다 |

### 7.3 이 문서가 주장하지 **않는** 것

- 우리 검색기가 SOTA 라고 주장하지 않는다. 28질의는 작은 표본이고, 신뢰구간을 계산하지 않았다.
- ①(전량 dense 교체)과 ③(형태소·SAC)의 **정확도 효과는 측정하지 않았다.** 진단만 실측이다.
- LegalBench-RAG·COLIEE 등 공개 벤치마크에 우리 시스템을 올려 비교한 적은 **없다**.
  한국 자치법규 대상 공개 검색 벤치마크가 부재한 것이 근본 원인이며,
  이는 그 자체로 **우리가 만들 수 있는 기여**이기도 하다(향후 과제).
- P@5 0.6643 은 여전히 **3분의 1이 오답**이라는 뜻이다. 갈 길이 남았다.

---

## 8. 참고문헌 (전부 원문 확인)

1. Pipitone, N., & Houir Alami, G. (2024). *LegalBench-RAG: A Benchmark for Retrieval-Augmented
   Generation in the Legal Domain.* arXiv:2408.10343.
2. Rosa, G. M., Rodrigues, R. C., Lotufo, R., & Nogueira, R. (2021).
   *Yes, BM25 is a Strong Baseline for Legal Case Retrieval.* arXiv:2105.05686. (COLIEE 2021 Task 1)
3. Reuter, M., Lingenberg, T., Liepiņa, R., Lagioia, F., Lippi, M., Sartor, G., Passerini, A.,
   & Sayin, B. (2025). *Towards Reliable Retrieval in RAG Systems for Large Legal Datasets.*
   arXiv:2510.06999. NLLP Workshop @ EMNLP 2025.
4. Magesh, V., Surani, F., Dahl, M., Suzgun, M., Manning, C. D., & Ho, D. E. (2024/2025).
   *Hallucination-Free? Assessing the Reliability of Leading AI Legal Research Tools.*
   arXiv:2405.20362; *Journal of Empirical Legal Studies*, doi:10.1111/jels.12413.
5. Nguyen, H.-T., Nguyen, T.-M., Le, X.-B., Le, T.-K., Nguyen, K.-H., Nguyen, H.-T., Vuong, T.-H.-Y.,
   & Nguyen, L.-M. (2025). *NOWJ@COLIEE 2025: A Multi-stage Framework Integrating Embedding Models
   and Large Language Models for Legal Retrieval and Entailment.* arXiv:2509.08025.
6. *An Overview of the COLIEE 2025 Competition: Legal Case Law and Statute Law Information Retrieval
   and Entailment.* Proceedings of ICAIL 2025 (ACM), doi:10.1145/3769126.3785016.
7. Han, H., Ma, L., Wang, Y., Shomer, H., et al. (2025).
   *RAG vs. GraphRAG: A Systematic Evaluation and Key Insights.* arXiv:2502.11371.
8. *When to use Graphs in RAG: A Comprehensive Analysis for Graph Retrieval-Augmented Generation.*
   arXiv:2506.05690. ICLR 2026. (GraphRAG-Bench; github.com/GraphRAG-Bench/GraphRAG-Benchmark)
9. nlpai-lab. *KURE: 고려대학교 한국어 검색 특화 임베딩 모델.* github.com/nlpai-lab/KURE
   (KURE-v1, 2024-12-21 공개; MTEB-ko-retrieval 리더보드).
10. *TWICE: What Advantages Can Low-Resource Domain-Specific Embedding Model Bring? — A Case Study
    on Korea Financial Texts.* arXiv:2502.07131.
11. Elastic. *Nori: The Official Elasticsearch Plugin for Korean Language Analysis.*
    elastic.co/blog/nori-the-official-elasticsearch-plugin-for-korean-language-analysis
12. OpenSearch. *Introducing reciprocal rank fusion for hybrid search.*
    opensearch.org/blog/introducing-reciprocal-rank-fusion-hybrid-search/

---

## 9. 관련 문서

- [01. 공모전 분석 및 수상전략](01_공모전_분석_및_수상전략.md)
- [02. 선행사례 및 유사서비스 분석](02_선행사례_및_유사서비스_분석.md)
- [03. 법령·조례 체계와 데이터소스](03_법령조례_체계와_데이터소스.md)
- [04. 선행연구 리뷰](04_선행연구_리뷰.md)
- [05. 시스템 설계](05_시스템_설계.md)
- [06. 실행계획 로드맵](06_실행계획_로드맵.md)
- [07. 레퍼런스 저장소 심층분석](07_레퍼런스_저장소_심층분석.md)
- [08. 데이터 확보 전략](08_데이터_확보_전략.md)
- [09. 설계 고도화 v2](09_설계_고도화_v2.md)
- [10. 시스템 구축 결과](10_시스템_구축_결과.md)
- [11. API키 발급가이드](11_API키_발급가이드.md)
- [12. 실데이터 수집결과](12_실데이터_수집결과.md)
- [13. 신경망 RAG 계층](13_신경망_RAG_계층.md) ← **직전 라운드. 이 문서는 그 후속이다.**
- [14. 법령그래프 표준 및 온톨로지](14_법령그래프_표준_및_온톨로지.md) ← §6-④ 그래프 재배치의 스키마 근거
- [README](README.md)
