# 조례↔예산 링크 표본검증 보고서

> **[2026-09-01 정정]** 본 보고서는 조례–예산 링크 93,964건 시점의 표본검증이다. 이후 링크가 183,145건으로 확장되어 신뢰도 0.8 이상 구간은 42,872건(23.4%)이 되었다. 정밀도 추정치(전체 64.9% / 고신뢰 구간 93.2%)는 표본 기준으로 그대로 유효하다.

- 대상: `ordinance_budget_link` 93,964건 (FY2025 51,992 / FY2026 41,972, 243개 지자체)
- 검증일: 2026-08-20
- 검증자: LLM 수작업 판정(단일 판정자). 판정 근거는 건별로 DB `evidence.audit` 에 기록
- 산출물: 본 문서 / 표본·판정 원자료 `F:/policy_maps/system/scratchpad/linkval/`
  (`sample.json`, `sample2.json`, `judge_input*.txt`, `judgments*.tsv`, `analyze*.py`, `final.py`)

---

## 0. 요약 (결론 먼저)

| 지표 | 값 |
|---|---|
| **전체 정밀도(엄격)** — 애매를 실패로 계산 | **64.9%** (95% CI 60.4~69.5) |
| **전체 정밀도(관대)** — 명백한 오답만 실패로 계산 | **80.8%** (95% CI 76.7~84.9) |
| 명백한 오매칭 비율(가중) | **19.2%** |
| **confidence ≥ 0.8 구간 정밀도(엄격)** | **93.2%** (95% CI 89.8~96.6), 잔존 18,374건 |
| confidence ≥ 0.8 구간 정밀도(관대) | 98.7% (95% CI 96.1~99.5) |
| confidence 의 판별력 (가중 AUC) | 0.770 |
| confidence 의 캘리브레이션 오차 (가중 ECE) | 0.095 |

**한 줄 요약: 링크 전체를 그대로 신뢰하면 안 된다(정밀도 65%). 그러나 confidence ≥ 0.8 로 자르면 18,374건이 남고 그 구간의 정밀도는 93%이며 명백한 오매칭은 1.3%에 불과하다. 따라서 "전량 노출"이 아니라 "임계값 적용 + 등급 표기"가 올바른 서비스 설계다.**

---

## 1. 표본설계

### 1.1 표본 크기 근거

단순무작위 기준 필요 표본 수(신뢰수준 95%, 허용오차 ±5%, 최대분산 p=0.5):

```
n0 = z²·p(1-p)/e² = 1.96² × 0.25 / 0.05² = 384.16 → 385
유한모집단 보정: n = n0 / (1 + (n0-1)/N) = 385 / (1 + 384/93,964) = 383.4 → 384
```

- **1차 표본 n = 464** (필요치 384를 상회). 층별 최소할당(12건) 때문에 384보다 커졌다.
- **2차(보충) 표본 n = 120** — 임계값 권고의 신뢰구간을 좁히기 위해 `confidence ≥ 0.8` 하위모집단(N = 18,374, 1차 표본 중복 제외 18,271)에서 단순무작위 추출.
- **총 판정 건수 584건.**

### 1.2 층화 설계

단순무작위가 아니라 **match_method × confidence 구간 층화**를 썼다. 이유는 두 가지다.

(a) `name+domain` 이 88,176건(93.8%)으로 지배적이어서 단순무작위로는 소수 방법(예: `name+article+dept` 692건)의 정밀도를 전혀 추정할 수 없다.
(b) confidence 캘리브레이션을 보려면 고신뢰 구간에도 표본이 필요한데, 비례배분만 하면 고신뢰 구간 표본이 부족하다.

- 지배층 `name+domain` 은 confidence 5구간(A [0.50,0.60) / B [0.60,0.70) / C [0.70,0.80) / D [0.80,0.90) / E [0.90,1.00])으로 세분해 방법 내부 비례배분(총 300, 구간별 최소 25)
- 소수 방법은 lo(<0.7)/hi(≥0.7) 2구간, 층당 12건 최소할당
- 극소 방법 5종(합계 32건, `*-fieldpenalty` 변종)은 `misc-fieldpenalty` 로 통합
- 총 18개 층, 난수 시드 20260820 고정(재현 가능)

**비례배분이 아니므로 전체 추정치는 반드시 설계가중(w_h = N_h / n_h)을 적용한 층화 추정량으로 계산했다.** 분산은 층화 유한모집단 보정(FPC) 포함, 부분모집단(method별·confidence 구간별) 추정은 선형화(Horvitz–Thompson + 층화 잔차) 방식.

| 층 | N_h | n_h | 층 | N_h | n_h |
|---|---:|---:|---|---:|---:|
| name+domain\|A | 39,703 | 135 | name+article\|hi | 753 | 12 |
| name+domain\|B | 20,477 | 70 | name+article\|lo | 139 | 12 |
| name+domain\|C | 11,214 | 38 | name+article+dept\|hi | 640 | 12 |
| name+domain\|D | 5,066 | 25 | name+article+dept\|lo | 52 | 12 |
| name+domain\|E | 11,716 | 40 | article-evidence\|hi | 357 | 12 |
| domain-topic\|* | 1,541 | 12 | article-evidence\|lo | 334 | 12 |
| name+domain-fieldpenalty\|* | 1,047 | 12 | article-evidence+dept\|hi | 317 | 12 |
| name+domain+dept\|lo | 266 | 12 | article-evidence+dept\|lo | 188 | 12 |
| name+domain+dept\|hi | 122 | 12 | misc-fieldpenalty\|* | 32 | 12 |

---

## 2. 판정 기준 (조작적 정의)

각 표본에 대해 **조례명·소관부서·조문 제목(있을 경우 최대 10개)** 과 **예산 세부사업명·분야/부문·부서코드·예산현액·회계연도** 를 함께 제시하고 3범주로 판정했다.

(지역 일치는 사전 검증 완료 — 93,964건 전부 `ordinances.region_id = budget_lines.region_id`, 불일치 0건.)

| 라벨 | 정의 | DB 반영 |
|---|---|---|
| **정답 (1)** | 해당 조례가 그 예산사업의 **직접 근거이거나 그 사업을 규율**한다. 실무자가 "이 조례에 근거해 이 사업을 편성했다"고 말할 수 있다. 조례의 목적·지원대상·사업범위가 예산사업의 내용을 포괄한다. | `verified = 1` |
| **애매 (0)** | 같은 정책영역이지만 **직접 근거로 단정할 수 없다**. 전형: ① 포괄적 기본조례 ↔ 특정 세부사업, ② 특화 조례 ↔ 상위 포괄사업, ③ 상위법·국고보조 지침이 주근거이고 조례는 부수적, ④ 기금조례 ↔ 일반회계 사업(회계 불일치), ⑤ 동일 시설 여부를 명칭만으로 확정 불가. | `verified = 0` (변경 없음) |
| **오답 (-1)** | 조례가 그 사업을 **규율하지 않는다**. 단순 단어 겹침, 대상 불일치, 절차·조직 조례를 사업 조례로 오인, 동종 다른 시설·기관. | `verified = -1` |

정밀도는 두 가지로 병기한다.

- **엄격(strict)**: 정답 / 전체 — 애매를 실패로 취급 (보수적 하한)
- **관대(lenient)**: (정답 + 애매) / 전체 — 명백한 오답만 실패 (상한)

이 둘을 모두 보고하는 이유는 "애매"가 서비스 관점에서 유해한지가 용도에 따라 다르기 때문이다. "근거 조례를 특정"하는 용도라면 애매는 실패이고, "관련 조례를 탐색"하는 용도라면 애매는 성공에 가깝다.

---

## 3. 결과

### 3.1 전체 정밀도 (층화 가중, n=464)

| 기준 | 추정치 | 표준오차 | 95% 신뢰구간 |
|---|---:|---:|---|
| 엄격(strict) | **0.6494** | 0.0232 | [0.6039, 0.6949] |
| 관대(lenient) | **0.8078** | 0.0208 | [0.7669, 0.8486] |

- 가중 라벨 구성: 정답 64.9% / 애매 15.8% / **오답 19.2%**
- 비가중 표본 구성: 정답 310건 / 애매 85건 / 오답 69건

### 3.2 층별 정밀도

| 층 | N_h | n_h | 엄격 | 관대 | 정답/애매/오답 | Wilson 95%(엄격) |
|---|---:|---:|---:|---:|---|---|
| name+domain\|A [0.50,0.60) | 39,703 | 135 | 0.422 | 0.696 | 57/37/41 | 0.34–0.51 |
| name+domain\|B [0.60,0.70) | 20,477 | 70 | 0.714 | 0.800 | 50/6/14 | 0.60–0.81 |
| name+domain\|C [0.70,0.80) | 11,214 | 38 | 0.868 | 0.947 | 33/3/2 | 0.73–0.94 |
| name+domain\|D [0.80,0.90) | 5,066 | 25 | 0.960 | 1.000 | 24/1/0 | 0.80–0.99 |
| name+domain\|E [0.90,1.00] | 11,716 | 40 | 0.950 | 0.950 | 38/0/2 | 0.83–0.99 |
| domain-topic\|* | 1,541 | 12 | **0.333** | 0.583 | 4/3/5 | 0.14–0.61 |
| name+domain-fieldpenalty\|* | 1,047 | 12 | 0.917 | 1.000 | 11/1/0 | 0.65–0.99 |
| name+domain+dept\|lo | 266 | 12 | 0.667 | 0.917 | 8/3/1 | 0.39–0.86 |
| name+domain+dept\|hi | 122 | 12 | 0.667 | 0.917 | 8/3/1 | 0.39–0.86 |
| name+article\|lo | 139 | 12 | 0.417 | 1.000 | 5/7/0 | 0.19–0.68 |
| name+article\|hi | 753 | 12 | 1.000 | 1.000 | 12/0/0 | 0.76–1.00 |
| name+article+dept\|lo | 52 | 12 | 0.750 | 1.000 | 9/3/0 | 0.47–0.91 |
| name+article+dept\|hi | 640 | 12 | 0.750 | 1.000 | 9/3/0 | 0.47–0.91 |
| article-evidence\|lo | 334 | 12 | 0.750 | 1.000 | 9/3/0 | 0.47–0.91 |
| article-evidence\|hi | 357 | 12 | 0.667 | 1.000 | 8/4/0 | 0.39–0.86 |
| article-evidence+dept\|lo | 188 | 12 | 0.583 | 0.833 | 7/3/2 | 0.32–0.81 |
| article-evidence+dept\|hi | 317 | 12 | 0.667 | 0.917 | 8/3/1 | 0.39–0.86 |
| misc-fieldpenalty\|* | 32 | 12 | 0.833 | 1.000 | 10/2/0 | 0.55–0.95 |

### 3.3 match_method 별 정밀도 (가중)

| method | N | n | 엄격 | 관대 | 95% CI(엄격) |
|---|---:|---:|---:|---:|---|
| name+domain | 88,176 | 308 | 0.648 | 0.803 | [0.600, 0.696] |
| domain-topic | 1,541 | 12 | **0.333** | 0.583 | [0.056, 0.611] |
| name+domain-fieldpenalty | 1,047 | 12 | 0.917 | 1.000 | [0.754, 1.000] |
| name+article | 892 | 24 | **0.909** | 1.000 | [0.866, 0.952] |
| name+article+dept | 692 | 24 | 0.750 | 1.000 | [0.515, 0.985] |
| article-evidence | 691 | 24 | 0.707 | 1.000 | [0.520, 0.893] |
| article-evidence+dept | 505 | 24 | 0.636 | 0.886 | [0.435, 0.837] |
| name+domain+dept | 388 | 24 | 0.667 | 0.917 | [0.462, 0.871] |
| `*-fieldpenalty` 극소 4종 | 28 | 9 | — | — | 표본 과소, 해석 불가 |

읽는 법:

- **`name+article` 가 가장 좋다(0.909).** 조례명 유사도와 조문 근거가 동시에 성립하는 링크다.
- **`domain-topic` 이 최악이다(0.333).** 12건 중 5건이 명백한 오답이었다. 이 방법은 "도메인 키워드 공유 + 토픽 유사"만으로 이어붙이므로 「자살예방 조례 ↔ 합천바캉스 축제」 같은 결과가 나온다.
- `article-evidence` 계열은 **엄격 기준은 낮지만(0.64~0.71) 관대 기준은 거의 1.0** 이다. 즉 "틀리진 않았는데 근거로 단정하기 애매한" 링크를 대량 생성한다. 전형적으로 포괄적 동물보호조례 ↔ 개별 동물시책 사업.

### 3.4 캘리브레이션 — confidence 는 실제 정확도를 반영하는가

| confidence 구간 | 모집단 N | n | 가중평균 conf | 실측 정밀도(엄격) | 실측 정밀도(관대) | 95% CI(엄격) |
|---|---:|---:|---:|---:|---:|---|
| [0.50, 0.55) | 22,895 | 84 | 0.521 | **0.380** | 0.587 | [0.266, 0.493] |
| [0.55, 0.60) | 19,706 | 95 | 0.575 | **0.477** | 0.805 | [0.368, 0.586] |
| [0.60, 0.65) | 12,949 | 66 | 0.623 | 0.684 | 0.810 | [0.546, 0.822] |
| [0.65, 0.70) | 8,228 | 56 | 0.675 | 0.760 | 0.800 | [0.609, 0.912] |
| [0.70, 0.80) | 11,812 | 60 | 0.746 | 0.865 | 0.947 | [0.761, 0.969] |
| [0.80, 0.90) | 5,636 | 41 | 0.836 | 0.950 | 1.000 | [0.878, 1.000] |
| [0.90, 1.00] | 12,738 | 41 | 0.960 | 0.936 | 0.941 | [0.853, 1.000] |

**판정: confidence 는 "순위 점수"로는 유효하지만 "확률"로는 부정확하다.**

- 판별력: 가중 AUC = **0.770** (무작위 0.5 대비 유의미하게 유효). confidence 로 정렬하면 좋은 링크가 위로 온다.
- 캘리브레이션: 가중 ECE = **0.095**. 특히 **저신뢰 구간에서 과신(overconfident)** 이 심하다. conf 0.52 라고 표시된 링크의 실제 정확도는 0.38이다. 반대로 중간 구간(0.60~0.80)은 과소평가(underconfident)다.
- 단조성은 성립한다(0.38 → 0.48 → 0.68 → 0.76 → 0.87 → 0.95 → 0.94). 0.90 이상에서 포화·소폭 반전.
- **결론: confidence 를 사용자에게 "정확도 %"로 그대로 보여주면 안 된다.** 구간 기반 등급(높음/보통/낮음) 또는 본 검증표로 재보정(isotonic / Platt)한 값을 써야 한다.

---

## 4. 임계값 권고

### 4.1 임계값별 정밀도와 잔존 링크수

| 규칙 | 잔존 링크 | 잔존 조례 | 정밀도(엄격) | 정밀도(관대) | 95% CI(엄격) |
|---|---:|---:|---:|---:|---|
| 전체 (conf ≥ 0.5) | 93,964 | 29,522 | 0.649 | 0.808 | [0.604, 0.695] |
| conf ≥ 0.7 | 30,186 | 12,426 | 0.911 | 0.960 | [0.860, 0.961] |
| **conf ≥ 0.8** | **18,374** | **7,955** | **0.932** † | **0.987** † | **[0.898, 0.966]** |
| conf ≥ 0.9 | 12,738 | 5,575 | 0.936 | 0.954 | [0.872, 1.000] |
| conf ≥ 1.0 | 1,959 | — | 0.947 | 1.000 | [0.891, 1.000] |
| name+domain & conf ≥ 0.8 | 16,782 | — | 0.953 | 0.965 | [0.900, 1.000] |
| article계열 전체 | 2,780 | — | 0.770 | 0.979 | [0.685, 0.854] |
| (article계열 conf≥0.7) OR (name+domain conf≥0.9) | 13,783 | — | 0.930 | 0.956 | [0.870, 0.990] |

† conf ≥ 0.8 행만 **1차 층화표본 부분(n=103) + 2차 보충 SRS(n=120) = n 223** 을 역분산 결합한 값이다. 개별로는 1차 부분 0.9405(SE 0.0253), 보충표본 0.9250(SE 0.0240, Wilson 95% [0.864, 0.960]). 보충표본 120건 중 **명백한 오답은 0건**이었고 나머지 9건은 전부 "애매"였다.

### 4.2 권고

**운영 임계값: `confidence ≥ 0.8`.**

- 정밀도 점추정 **93.2%**, 95% 신뢰구간 하한 **89.8%**.
  → "정밀도 90% 이상"이라는 목표를 **점추정으로는 충족하나 신뢰구간 하한이 89.8%로 90%에 0.2%p 미달한다.** 이것을 90% 달성으로 반올림해 주장하지 않는다. 정확한 표현은 **"정밀도 93%(95% CI 89.8~96.6), 명백한 오매칭 1.3%(95% CI 0.5~3.9)"** 이다.
- 이 임계값에서 **18,374건(19.6%)** 이 남고, **7,955개 조례**가 예산과 연결된 상태로 유지된다.
- confidence ≥ 0.9 로 더 올려도 정밀도는 0.936으로 사실상 개선되지 않는데(포화) 링크는 12,738건으로 31% 줄어든다. **0.8 이 정밀도–재현율 균형점이다.**

**method 결합 규칙(선택적 강화)** — conf ≥ 0.8 구간을 method별로 쪼개면(1차 + 보충 n=223, 단순비율):

| method | N(conf≥0.8) | n | 정답/애매/오답 | 엄격 |
|---|---:|---:|---|---:|
| name+domain | 16,782 | 174 | 162/10/2 | 0.931 |
| name+article | 572 | 13 | 13/0/0 | 1.000 |
| name+article+dept | 543 | 12 | 10/2/0 | 0.833 |
| article-evidence+dept | 199 | 10 | 7/3/0 | 0.700 |
| article-evidence | 226 | 10 | 6/4/0 | 0.600 |
| name+domain+dept | 52 | 4 | 4/0/0 | 1.000 |

→ conf ≥ 0.8 구간에서도 `article-evidence` 계열은 엄격 정밀도가 낮다(0.60~0.70). 다만 **오답은 0건**이고 전부 "애매"이므로, 이들은 삭제 대상이 아니라 **"관련 조례" 등급으로 분리 표기**하는 것이 맞다.

**`domain-topic` 은 임계값과 무관하게 제외 권고.** 1,541건 전부 confidence ≤ 0.55 이며 정밀도 0.333이다.

### 4.3 서비스 노출 등급 (권고안)

| 등급 | 조건 | 링크수 | 기대 정밀도 | UI 문구 |
|---|---|---:|---:|---|
| A. 근거 조례 | conf ≥ 0.8 AND method ∈ {name+article(+dept), name+domain(+dept)} | ~17,949 | ~0.93 | "이 사업의 근거 조례로 추정" |
| B. 관련 조례 | conf ≥ 0.8 AND method ∈ {article-evidence 계열} | ~425 | 엄격 0.65 / 관대 1.00 | "관련 조례(직접 근거가 아닐 수 있음)" |
| C. 참고 | 0.7 ≤ conf < 0.8 | 11,812 | ~0.87 | 접힘 처리, 클릭 시 노출 |
| X. 비노출 | conf < 0.7 또는 method = domain-topic | 63,778 | ~0.50 | 검색 인덱스에서 제외 |

---

## 5. 오매칭 원인 유형화 (표본 내 오답 69건 전수 분류)

| 유형 | 건수 | 비중 | 대표 사례 |
|---|---:|---:|---|
| **① 대상·수혜자 불일치** | 15 | 21.7% | 「장애인**공무원** 편의지원 조례」 ↔ 「장애인연금 지원」 / 「**가정 밖** 청소년 조례」 ↔ 「**학교 밖** 청소년 지원」 / 「**고령**농업인 지원 조례」 ↔ 「농업인대학 운영」 / 「**스마트**농업 육성 조례」 ↔ 「**도시**농업 육성지원」 |
| **② 절차·조직 조례를 사업 조례로 오인** | 13 | 18.8% | 「공동주택 관리 **감사** 조례」 ↔ 「공동주택 **지원**사업」(3건 반복) / 「장애인복지**위원회** 운영 조례」 ↔ 「장애인복지관 운영(25억)」 / 「보건소 **수가** 조례」 ↔ 「국가예방접종사업」·「보건소 그린리모델링」 / 「시청·행정복지센터 **소재지** 조례」 ↔ 「주거복지센터」 |
| **③ 일반명사 문자열 겹침** | 13 | 18.8% | 「자살예방 조례」 ↔ 「대구생활**문화**센터 운영」 / 「**마을**자치연금 조례」 ↔ 「**마을**기업 육성」 / 「반려식물**문화** 조례」 ↔ 「**문화**도시 조성」 / 「**문화상** 조례(시상제도)」 ↔ 「**문화상**품화사업」(문자열 오분해) / 「주거**환경 정비** 조례」 ↔ 「**환경정비**(청소)」(동음이의) |
| **④ 동종 다른 시설·기관** | 11 | 15.9% | 「**소수**박물관 조례」 ↔ 「**인삼**박물관 운영」 / 「**진보**문화체육센터 조례」 ↔ 「**청송국민**체육센터」 / 「공공**심야어린이병원** 조례」 ↔ 「공공**어린이재활병원**」 / 「문화의 **거리** 조례」 ↔ 「문화의 **집** 운영」 / 「체육진흥**협의회** 조례」 ↔ 「구**체육회** 운영지원」 |
| **⑤ 주제 완전 불일치** | 8 | 11.6% | 「교복 지원 조례」 ↔ 「종합사회복지관 운영(20억)」 / 「농업기계 임대사업 조례」 ↔ 「농업인대학 운영」 / 「감정노동자 보호 조례」 ↔ 「생태문화 조성」 / 「자살예방 조례」 ↔ 「합천바캉스 축제」 |
| **⑥ 별개 근거법령·제도의 사업** | 7 | 10.1% | 「아동친화도시 조례」 ↔ 「아동수당」(아동수당법 법정급여) / 「장애인 생활지원금 조례」 ↔ 「중증장애인 자립생활지원」(별도 국고보조) / 「전통시장 **자율**소방대 조례」 ↔ 「**의용**소방대 지원」 |
| **⑦ 부서 일반경비** | 2 | 2.9% | 「양산시 동물보호 조례」 ↔ 「**기본경비**(동물보호과)」 — 부서 운영비는 어떤 조례의 근거사업도 아니다 |

### 5.1 원인의 구조적 진단

- ①③④⑤ 합계 **47건(68%)** 은 전부 **이름 유사도만으로 이어붙인 결과**다. 실제로 오답 69건의 method 분포는 `name+domain` 59건 · `name+domain+dept` 2건(**합 61건 = 88.4%**) · `domain-topic` 5건 · `article-evidence+dept` 3건이며, 그중 **41건이 최저 신뢰구간 `name+domain|A`(conf < 0.6)**, 14건이 `name+domain|B`(0.60~0.70) 에서 나왔다. confidence 로 보면 **오답 69건 중 47건(68.1%)이 conf < 0.6, 63건(91.3%)이 conf < 0.7, 67건(97.1%)이 conf < 0.8 구간에 몰려 있다.**
  → 저신뢰 이름매칭이 오류의 압도적 주범이다. 임계값 하나로 대부분 제거된다.
- ②는 성격이 다른 결함이다. **조례의 "성격"(사업조례 / 절차·조직조례 / 감사조례 / 수수료·수가조례)을 구분하지 않은 것**이 원인이며, 이름 유사도만으로는 절대 잡히지 않는다. 「공동주택 관리 감사 조례」와 「공동주택 지원사업」은 이름이 매우 비슷하다.
- ⑥은 상위법 위임 정보를 쓰지 않은 결과다. DB에 `delegations` 3,203건과 `instrument_relations(CITES)` 5,377건이 이미 있으므로 활용 여지가 있다.
- ⑦은 예산사업명 사전 필터로 즉시 제거 가능하다("기본경비", "인력운영비", "행정운영경비", "부서운영" 등).

### 5.2 "애매(85건)"의 성격

애매 판정은 오류가 아니라 **모델링 한계**다. 주된 패턴:

- 포괄적 기본조례 ↔ 특정 세부사업 (「저출산·고령사회정책 조례」 ↔ 「임산부 영유아 관리」)
- 특화 조례 ↔ 상위 포괄사업 (「백일해 예방접종 지원 조례」 ↔ 「예방접종」 1.08억)
- 국고보조 법정사업 (「보육 조례」 ↔ 「보육교직원 인건비」 4,884억 — 근거는 영유아보육법)
- 회계 불일치 (기금 설치조례 ↔ 일반회계 사업)

→ 이 유형은 **조문 본문 매칭으로 상당 부분 해소 가능**하다(§7-6 참조).

---

## 6. DB 반영

`ordinance_budget_link.verified` 갱신 완료. 50건 단위 짧은 `BEGIN IMMEDIATE` 트랜잭션 + `busy_timeout = 60s`(본문 수집 배치와의 쓰기 경합 회피). 각 행의 `evidence` JSON 에 `audit` 키를 추가해 판정 라벨·근거·표본출처·판정자를 기록했다.

| verified | 건수 | 의미 |
|---:|---:|---|
| 1 | 421 | 표본검증 정답 |
| 0 | 93,474 | 미검증(93,380) + 표본검증 "애매"(94) |
| -1 | 69 | 표본검증 오답 |

**주의(코드 영향):** `policymap/mcp_server/server.py` 의 `bool(r.get("verified"))`(1562행 부근)와 `verified_links += 1` 분기(1534·1547행)는 `verified = -1` 을 **참(True)** 으로 평가한다. 즉 지금 상태에서는 오답 69건이 "검증된 링크"로 집계된다. **반드시 `verified == 1` 비교로 수정해야 한다.** (본 작업 범위 밖 파일이라 수정하지 않고 보고만 한다.)

`policymap/graph/analysis.py:1178` 의 `COALESCE(verified,0)=0` 조건은 오답(-1)을 재계산·프루닝 대상에서 제외하므로 동작상 오류는 없으나, 오답으로 확정된 링크는 별도로 정리(삭제 또는 영구 배제)하는 편이 낫다.

---

## 7. 개선 권고 (우선순위)

1. **[즉시] 임계값 `confidence ≥ 0.8` 을 서비스 기본값으로 적용.** 정밀도 65% → 93%. 링크 93,964 → 18,374. `domain-topic`(1,541건)은 임계값과 무관하게 전량 제외.
2. **[즉시] `verified == 1` 비교 버그 수정**(server.py). 현재 오답이 검증완료로 집계된다.
3. **[즉시] 예산사업명 블랙리스트 필터.** "기본경비", "인력운영비", "행정운영경비", "부서운영", "예비비", "○○과 운영" 패턴은 어떤 조례의 근거사업도 아니다.
4. **[단기] 조례 성격 분류기 추가.** 조례명 접미 패턴으로 `사업조례 / 위원회·협의회 조례 / 감사조례 / 수수료·수가조례 / 소재지·정원조례` 를 분류하고, **사업조례가 아닌 조례는 이름매칭 채널에서 감점 또는 배제.** 오답 69건 중 13건(18.8%)이 이 한 규칙으로 제거된다.
5. **[단기] confidence 재보정.** 본 검증 584건을 학습셋으로 isotonic regression 을 적합해 `confidence → 실측 정밀도` 로 변환. 현재 ECE 0.095, 특히 저신뢰 구간 과신이 심하다. 재보정 후에는 사용자에게 "정확도 약 XX%"를 정직하게 표시할 수 있다.
6. **[중기] 조문 본문 기반 매칭으로 전환.** 현재 링크된 조례 29,522개 중 조문을 보유한 것은 1,384개(4.7%)뿐이며, 링크 93,964건 중 조문 보유는 6,252건(6.7%)이다. 진행 중인 본문 156,027건 전수 수집이 완료되면 `name+article` 채널(현재 정밀도 0.909, 최고 성능)의 적용 범위가 수십 배로 넓어진다. **이것이 정밀도를 근본적으로 올리는 유일한 경로다.**
7. **[중기] 상위법 위임정보 활용.** `delegations`(3,203) · `instrument_relations`(5,377)로 "국고보조 법정사업 ↔ 지자체 조례" 오매칭(유형 ⑥)을 걸러낸다.
8. **[중기] "근거 / 관련" 2등급 노출.** article-evidence 계열은 오답이 0건이지만 직접 근거로는 애매하다. 삭제하지 말고 별도 등급으로 보여주면 사용자 가치는 유지하면서 정확성 주장은 정직해진다.
9. **[검증 체계] 2차 검증 라운드.** 본 검증은 단일 판정자(LLM)이므로 판정자 간 신뢰도(κ)를 측정하지 못했다. 지자체 실무자 또는 제2 판정자로 표본 100건을 교차판정해 κ 를 보고하면 방법론 신뢰도가 올라간다.

---

## 8. 이 검증의 한계 (정직한 명시)

- **단일 판정자.** 판정자 간 일치도(Cohen kappa)를 측정하지 못했다. "애매" 판정 경계는 판정자에 따라 이동할 수 있고, 엄격/관대 두 값을 병기한 것은 이 불확실성을 구간으로 드러내기 위함이다.
- **원문 미확인.** 조례 본문을 보유한 표본은 소수(링크 기준 6.7%)이며, 대부분은 **조례명·조문 제목·예산사업명**만으로 판정했다. 실제 조례 본문에 해당 사업의 근거 조항이 있는데 "애매"로 처리한 사례가 있을 수 있다(즉 엄격 정밀도는 **하한**이다).
- **예산 편성의 실제 근거는 관찰 불가.** 예산서에는 근거 법령·조례가 명시되지 않는다. 본 검증은 "이 조례가 이 사업을 규율하는가"라는 **규범적 판단**이지 "실제로 이 조례를 인용해 편성했는가"라는 **사실 확인**이 아니다.
- **소수 층의 신뢰구간이 넓다.** `domain-topic`(n=12) 정밀도 0.333의 95% CI는 [0.056, 0.611]이다. "0.333"을 정확한 값으로 인용하면 안 되며, "매우 낮다"는 방향성만 신뢰할 수 있다.
- **FY2025 / FY2026 두 회계연도 혼재.** 연도별 정밀도 차이는 별도 분석하지 않았다.

---

## 부록. 재현 절차

```bash
cd F:/policy_maps/system
python scratchpad/linkval/sample.py     # 1차 층화표본 464건 (seed 20260820)
python scratchpad/linkval/dump.py       # 판정용 컨텍스트 생성
python scratchpad/linkval/sample2.py    # 2차 보충 SRS 120건 (seed 20260821)
# → judgments.tsv / judgments2.tsv 에 수작업 판정 기록
python scratchpad/linkval/analyze.py    # 층화 가중 추정
python scratchpad/linkval/analyze2.py   # conf>=0.8 결합 추정
python scratchpad/linkval/final.py      # AUC / ECE / method별
python scratchpad/linkval/apply_db.py   # verified 반영
```
