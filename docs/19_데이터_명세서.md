# 19. 데이터 명세서 (프론트엔드용)

> 대상 독자: 프론트엔드 개발자(하수범). DB·파이프라인에 접근하지 않고 **정적 JSON 번들만으로** 시각화를 구현할 수 있게 하는 것이 이 문서의 목적이다.
>
> **작성 원칙**: 아래 모든 스키마·수치는 2026-08-21 실제 파일을 읽어 확인한 값이다. 확인하지 못한 항목은 `(확인 필요)` 로 표기했고, 추측으로 채운 필드는 없다.
>
> 데이터 기준일(`as_of_date`): **2026-08-21** / `stale`: **false** / `stale_days` 임계값: **8**

---

## 0. 30초 요약 — 프론트가 실제로 읽을 파일

| 우선순위 | 파일 | 크기 | 용도 |
|---|---|---:|---|
| ★★★ | `system/data/regions/index.json` | 25KB | 지역 목록(284개). 첫 화면 진입점 |
| ★★★ | `system/data/regions/{sig_cd}.json` | 3~5KB × 284 | 지역 상세 카드 |
| ★★★ | `viz/public/geo/municipalities.geojson` | 1.1MB | **지도 경계 폴리곤(227개, V-World 2026-09-01)** |
| ★☆☆ | `system/data/reference/skorea-municipalities-2018-geo.json` | 18MB | 법정동코드 크로스워크 교차검증용(2026-09-01 이전 지도 경계) |
| ★★★ | `system/data/reference/kostat_to_bjd.json` | 108KB | **위 GeoJSON 코드 → sig_cd 변환표(필수)** |
| ★★☆ | `system/data/manifest.json` | 157KB | 번들 목차·전체 카운트·워터마크 |
| ★★☆ | `system/data/changes/latest.json` | 100KB | 최근 변경 200건 |
| ★☆☆ | `system/data/meta/graph-stats.json` | 796B | 그래프 규모 통계(수치 배지용) |
| ⚠️ | `system/data/graph/nodes.json` | **134MiB** | 브라우저 직접 로드 **불가** → §7 참조 |
| ⚠️ | `system/data/graph/edges.json` | **209MiB** | 브라우저 직접 로드 **불가** → §7 참조 |

> **지도를 그리려면 GeoJSON(`code`=11010 통계청코드)과 우리 데이터(`sig_cd`=11110 법정동코드)를 반드시 크로스워크해야 한다.** 두 코드는 다르다. §2.7 참조.

---

## 1. 폴더 구조도

```
F:/policy_maps/                             ← 프로젝트 루트 (git 저장소 아님, 원격 없음)
│
├── README.md, 01~18_*.md, 20_깃허브_공개계획.md   [문서] 기획·연구·검증 보고서 20종
├── 19_데이터_명세서.md                             [문서] ← 이 파일
├── 1./2./3. 경진대회_*.hwp/.png                    [문서] 대회 공식 서류
├── .gitignore
│
├── external/                              60MB  [참고] 레퍼런스 저장소 클론
│   ├── korea100/                                (데이터 계약·verification 3단계 원본)
│   └── korea100studio/
│
├── viz/                                   1.1MB [프론트] ★ 프론트엔드 작업 영역
│   ├── mock/generate_mock.py                    (목업 생성기)
│   └── public/data/                             (system/data 번들의 축소 미러)
│       ├── manifest.json  api/{diffusion,effectiveness,gap,peers,search,votes}.json
│       ├── regions/11000.json  graph/{nodes,edges}.json  changes/  meta/  state/
│
└── system/                                      [코드] 백엔드 본체
    ├── README.md · CONTRACTS.md(22KB) · RUNBOOK.md(23KB)
    ├── pyproject.toml · requirements-core.txt · requirements-full.txt · .env(.example)
    │
    ├── db/schema.sql                       40KB  DDL. CREATE TABLE 30개(+런타임 3 = 실DB 33)
    │
    ├── policymap/                         2.7MB  파이썬 패키지 40파일 / 22,525줄
    │   ├── config.py · db.py · util.py · run.py(61KB CLI) · standards.py(44KB ELI/AKN)
    │   ├── reference.py · seed_korea100.py
    │   ├── collectors/  법령API·국회API·지방재정365·경계 수집기 (5모듈)
    │   ├── parsers/     조문·카테고리·위임·임베딩 파서 (4모듈)
    │   ├── graph/       build.py · analysis.py · export.py  ← 정적 번들 생성기
    │   ├── analytics/   base · peers(유사지자체) · spatial(Moran) · diffusion · eha
    │   ├── neural/      embeddings.py · gnn.py (node2vec/metapath2vec/graphsage)
    │   ├── rag/         index · retrieve · rerank · community · evalset
    │   └── mcp_server/server.py           130KB  ← MCP tool 14종 (2,518줄)
    │
    ├── tools_*.py                          10개  일회성 수집·검증 스크립트
    ├── tests/                              705KB pytest 78개 (전부 통과, 282초)
    │
    └── data/                              ~14GB  ★ 데이터
        ├── policymap.db                   4.6GB  SQLite 단일 진실원천(SSOT), 33테이블
        ├── policymap.db-shm / -wal               WAL 부속(복사 시 함께 복사할 것)
        ├── manifest.json                  157KB  번들 목차
        ├── regions/                       1.5MB  index.json + {sig_cd}.json 284개
        ├── graph/                         343MB  nodes.json(134MiB) + edges.json(209MiB)
        ├── changes/                        11MB  latest.json + feed-2026-08.json
        ├── meta/graph-stats.json           796B
        ├── state/watermarks.json          228KB  수집 파티션 498개 상태
        ├── reference/                      21MB  ★ 지도·크로스워크 원천
        │   ├── skorea-municipalities-2018-geo.json   18MB  크로스워크 교차검증용(구 경계 250)
        │   ├── kostat_to_bjd.json                   108KB  통계청코드→sig_cd (250건 전수검증)
        │   ├── regions_bjd.json                     129KB  법정동코드 파생 537행
        │   ├── legal_dong_codes.txt                 3.2MB  법정동코드 원본
        │   ├── population_sigungu.csv / population_haengjeonggu.csv / jumin_population_raw_202607.csv
        │   ├── reorg_events.json / reorg_successors_resolved.json  행정구역 개편·승계
        │   └── geojson_crosswalk_plan.md
        ├── index/                         9.5GB  RAG 인덱스
        │   ├── all/  meta.json + seg-000~009 (10세그먼트, 2,451,813문서)
        │   └── communities/  ordinance_similarity.json(89KB) · region_adjacency.json(26KB)
        └── *.log / *_status.json / final_report.json   파이프라인 실행기록
```

**프론트가 절대 건드리지 않아도 되는 것**: `policymap.db`, `index/`(9.5GB RAG), `policymap/` 코드.
**프론트가 실제로 소비할 것**: `data/regions/`, `data/reference/`, `data/manifest.json`, `data/changes/`, `data/meta/`, (샤딩 후) `data/graph/`.

---

## 2. 정적 번들 JSON 스키마

번들 스키마 식별자: `"policymap.static.v1"` · 생성기: `policymap/graph/export.py::export_static`
파일 개수: manifest 등록 **290개** (regions 285 + graph 2 + changes 2 + meta 1) · 등록 총량 **343MiB**

### 2.1 `manifest.json` — 번들 목차

| 필드 | 타입 | 예시값 | 설명 |
|---|---|---|---|
| `schema` | string | `"policymap.static.v1"` | 번들 스키마 버전. 다르면 파서 갱신 필요 |
| `generated_at` | string(ISO8601+09:00) | `"2026-08-21T08:49:35+0900"` | 번들 생성 시각 |
| `as_of_date` | string(YYYY-MM-DD) | `"2026-08-21"` | **데이터 기준일. 모든 화면에 노출 필수** |
| `stale` | boolean | `false` | true면 신선도 임계 초과 → 경고 배너 |
| `stale_days` | number | `8` | 신선도 임계값(일) |
| `counts` | object | 아래 | 엔터티 총량 |
| `graph_stats` | object | 아래 | 그래프 노드/엣지 통계 |
| `watermarks` | array(498) | 아래 | 수집 파티션 상태 요약 |
| `region_index` | string | `"regions/index.json"` | 지역 인덱스 상대경로 |
| `changes_latest` | string | `"changes/latest.json"` | 최근 변경 상대경로 |
| `changes_months` | array(1) | `[{month:"2026-08",count:23066,file:"changes/feed-2026-08.json"}]` | 월별 변경 피드 |
| `files` | array(290) | `{file,bytes,rows,hash}` | 전 파일 목록 + sha256 |

`counts` 실측값:

| 키 | 값 |
|---|---:|
| `regions` | 556 |
| `legal_instrument` | 29,811 |
| `ordinances` | 199,858 |
| `delegations` | 421,627 |
| `bills` | 19,847 |
| `budget_lines` | 933,527 |
| `change_log` | 23,066 |
| `graph_nodes` | 250,416 |
| `graph_edges` | 1,125,036 |

`graph_stats` 실측값 (= `meta/graph-stats.json` 과 동일 + `total_nodes`/`total_edges` 추가):

```jsonc
{
  "backend": "networkx",
  "node_counts": { "Region":556, "LegalInstrument":29811, "Ordinance":199858,
                   "Bill":19847, "Legislator":320, "Party":8, "Category":16,
                   "BudgetLine":0 },              // ← BudgetLine 노드는 0개다
  "edge_counts": { "HAS_ORDINANCE":199858, "DELEGATED_FROM/SUBORDINATE_TO":307779,
                   "CITES(+relations)":2189, "ADJACENT_TO":1098, "SUCCEEDED_BY":17,
                   "CONTAINS":529, "SIMILAR_TO":21740, "IN_CATEGORY":334831,
                   "FUNDED_BY":0, "PROPOSED_BY":241546, "VOTED":57178,
                   "MEMBER_OF":299, "ENACTS":0 },
  "skipped_edges": { "DELEGATED_FROM":113848, "CITES":810150, "FUNDED_BY":183145 }
}
```

> ⚠️ **`skipped_edges` 를 반드시 읽어라.** 엣지가 통째로 빠져 있다.
> - `FUNDED_BY` **183,145건 전량 제외** — 조례↔예산 링크는 **정적 그래프 번들에 없다**. 예산 연계 화면은 `regions/{sig_cd}.json` 의 `budget` 집계 또는 MCP `ordinance_effectiveness` 로 가야 한다.
> - `CITES` 810,150건 제외(그래프에는 1,751건만) — 인용 네트워크를 그래프 번들로 그릴 수 없다.
> - `DELEGATED_FROM` 113,848건 제외(266,171건만 수록) — 대상 노드가 그래프에 없는 위임(미수집 상위법 등)이 잘린 것.

```typescript
export interface Manifest {
  schema: "policymap.static.v1";
  generated_at: string;          // ISO8601 +09:00
  as_of_date: string;            // "YYYY-MM-DD"
  stale: boolean;
  stale_days: number;
  counts: {
    regions: number; legal_instrument: number; ordinances: number;
    delegations: number; bills: number; budget_lines: number;
    change_log: number; graph_nodes: number; graph_edges: number;
  };
  graph_stats: {
    backend: string;
    node_counts: Record<string, number>;
    edge_counts: Record<string, number>;
    skipped_edges: Record<string, number>;
  };
  watermarks: WatermarkBrief[];
  region_index: string;
  changes_latest: string;
  changes_months: { month: string; count: number; file: string }[];
  files: { file: string; bytes: number; rows: number | null; hash: string }[];
}

export interface WatermarkBrief {
  source: "budget" | "ordin" | "ordin_meta" | "ordin_body" | "na_bill"
        | "na_vote" | "category_clf" | "deleg_backfill" | string;
  scope: string;                 // "laf:1100000|fyr:2025" 등
  cursor: string | null;
  status: "ok" | "partial" | "error" | "stale" | "manual-hold";
  last_success: string | null;   // ISO8601
  changed: number;
}
```

**watermarks 실측 분포** — 전 498건 `status:"ok"` (오류 파티션 0건).
source별: `budget` 488 · `na_bill` 2 · `na_vote` 2 · `ordin_meta` 2 · `category_clf` 1 · `deleg_backfill` 1 · `ordin` 1 · `ordin_body` 1.

---

### 2.2 `regions/index.json` — 지역 목록 (25KB)

| 필드 | 타입 | 예시 | 설명 |
|---|---|---|---|
| `as_of_date` | string | `"2026-08-21"` | |
| `stale` | boolean | `false` | |
| `count` | number | `284` | items 길이 |
| `items[].sig_cd` | string(5) | `"11110"` | **조인 키.** 상세 파일명과 동일 |
| `items[].name` | string | `"종로구"` | 최하위 지명 |
| `items[].level` | number | `2` | 1 광역 / 2 기초 / 3 행정시·일반구 |
| `items[].ordinance_total` | number | `500` | 현행 자치법규 수 |
| `items[].file` | string | `"regions/11110.json"` | 상세 파일 상대경로 |

**level 분포(실측)**: `1`=16, `2`=227, `3`=41 → 합 284.
> ⚠️ `manifest.counts.regions` 는 **556** 이지만 번들에 파일로 나온 지역은 **284**뿐이다. 차이는 폐지·통합 지역(abolished 245 / merged 6 / renamed 2)과 **교육청(level=4, 19개)** 이 export에서 제외된 것이다. 프론트는 284개만 다룬다고 가정하면 된다. level=3(일반구·행정시 41개)은 **자치입법권이 없어(`has_legislation=0`) 조례가 0건**이므로 지도 코로플레스에서 회색 처리 또는 상위 시(市)로 합산해야 한다.

```typescript
export interface RegionIndex {
  as_of_date: string; stale: boolean; count: number;
  items: RegionIndexItem[];
}
export interface RegionIndexItem {
  sig_cd: string; name: string;
  level: 1 | 2 | 3;
  ordinance_total: number;
  file: string;                  // "regions/{sig_cd}.json"
}
```

---

### 2.3 `regions/{sig_cd}.json` — 지역 상세 (284개, 각 3~5KB)

실측 예시: `regions/11110.json`(서울 종로구)

| 필드 | 타입 | 예시값 | 설명 |
|---|---|---|---|
| `sig_cd` | string(5) | `"11110"` | 시군구코드 |
| `region_id` | string | `"11110"` | 정규 ID(법정동코드 앞자리). 광역은 2자리(`"11"`) |
| `name` | string | `"종로구"` | |
| `full_name` | string | `"서울특별시 종로구"` | 화면 표시용 |
| `level` | number | `2` | |
| `status` | string | `"active"` | `active`\|`abolished`\|`merged`\|`renamed` |
| `population` | number\|null | `136139` | 주민등록 인구 |
| `ordinance_kinds` | object | `{"조례":386,"규칙":114}` | **키가 한글**이며 지역마다 키 집합이 다르다 |
| `ordinance_total` | number | `500` | 현행 합계 |
| `top_categories` | array | `[{code:"C01",count:190}, …]` | count 내림차순. 최대 12~14개 |
| `budget` | object | `{lines:2331, exe_amt:1143283575903, budget_now:1882818273920}` | 원 단위 정수 |
| `recent_changes` | array | 아래 | 최근 변경 (실측 6건) |
| `as_of_date` | string | `"2026-08-19"` | ⚠️ manifest(`2026-08-21`)와 **다를 수 있다** |
| `stale` | boolean | `false` | |

`recent_changes[]` 필드 7개: `change_id`, `ts`, `entity_type`, `entity_id`, `entity_name`, `event`, `official_url`.

```jsonc
{
  "change_id": "5252db13877d4403b3b5a299317146de",
  "ts": "2026-08-19T11:16:04+0900",
  "entity_type": "ordinance",              // "ordinance" | "budget" | ...
  "entity_id": "ordin:2088635",
  "entity_name": "서울특별시 종로구 아동 보호 및 지원에 관한 조례",
  "event": "created",                      // "created"|"amended"|"repealed"|"budget_changed"
  "official_url": "https://www.law.go.kr/DRF/lawService.do?OC={LAW_OC}&target=ordin&MST=2088635&type=HTML&mobileYn="
}
```

> ⚠️ **`official_url` 에 API 키(`OC={LAW_OC}`)가 박혀 있다.** 공개 배포 전 반드시 제거하거나 `https://www.law.go.kr/ordinInfoP.do?ordinSeq={mst}` 형태의 canonical URL로 치환할 것. 그대로 노출하면 키가 유출된다.

**카테고리 코드표(실측 16종, DB `categories` 테이블 전수)**

| code | name | | code | name |
|---|---|---|---|---|
| C01 | 행정·자치·의회 | | C09 | 도시·건축·주택 |
| C02 | 재정·세무·회계 | | C10 | 교통 |
| C03 | 복지·돌봄 | | C11 | 경제·산업·일자리 |
| C04 | 인구·출산·양육 | | C12 | 농림·수산 |
| C05 | 청년·교육 | | C13 | 문화·체육·관광 |
| C06 | 보건·의료 | | C14 | 동물·반려 |
| C07 | 환경·기후 | | C-BIRTH | 출산장려ㆍ양육지원 |
| C08 | 안전·재난 | | C-PET | 반려동물ㆍ동물보호 |

> `C-BIRTH`/`C-PET` 는 시드 잔재로 `C04`/`C14` 와 의미가 겹친다. 그래프 노드에는 16개가 모두 있으나 `regions/*.json` 의 `top_categories` 에서는 `C01~C13` 만 관측됐다. 범례는 **C01~C14 만** 쓰고 `C-*` 는 숨기는 것을 권한다.

```typescript
export interface RegionDetail {
  sig_cd: string; region_id: string;
  name: string; full_name: string;
  level: 1 | 2 | 3;
  status: "active" | "abolished" | "merged" | "renamed";
  population: number | null;
  ordinance_kinds: Record<string, number>;   // {"조례":386,"규칙":114}
  ordinance_total: number;
  top_categories: { code: string; count: number }[];
  budget: {
    lines: number;        // 세부사업 행 수
    exe_amt: number;      // 지출액 합계 (원)
    budget_now: number;   // 예산현액 합계 (원)
  };
  recent_changes: ChangeItem[];
  as_of_date: string;
  stale: boolean;
}
export interface ChangeItem {
  change_id: string; ts: string;
  entity_type: string; entity_id: string; entity_name: string;
  event: "created" | "amended" | "repealed" | "budget_changed" | string;
  official_url: string | null;
}
```
집행률 계산: `exec_rate = budget.exe_amt / budget.budget_now` (종로구 실측 = 0.607).

---

### 2.4 `graph/nodes.json` — 그래프 노드 (250,416개 / 134MiB)

최상위: `{ as_of_date, stale, count, nodes: [...] }`

**공통 필드**: `id`(`"{kind}:{src_id}"`), `name`, `label`, `kind`, `src_id`.
**label 실측 분포(전수 스캔)**: `Ordinance` 199,858 · `LegalInstrument` 29,811 · `Bill` 19,847 · `Region` 556 · `Legislator` 320 · `Category` 16 · `Party` 8. → **`BudgetLine` 노드는 0개**.

label별 고유 필드(실측 샘플):

```jsonc
// Region (556)
{ "id":"region:11", "name":"서울특별시", "full_name":"서울특별시", "level":1,
  "sig_cd":"11000", "has_legislation":1, "status":"active",
  "label":"Region", "kind":"region", "src_id":"11" }

// Ordinance (199,858)
{ "id":"ordinance:ordin:2146281", "name":"경산시 출산장려 지원에 관한 조례",
  "org_name":"경상북도 경산시", "ord_kind":"조례", "local_tier":"L1",
  "delegation_type":"law-delegated", "department":"건강증진과",
  "enacted_on":"20260708", "effective_on":"20260708", "rr_cls_cd":"일부개정",
  "article_count":15, "status":"active", "verification_status":"source-linked",
  "label":"Ordinance", "kind":"ordinance", "region_id":"47290", "src_id":"ordin:2146281" }

// LegalInstrument (29,811)
{ "id":"instrument:statute:257787", "name":"112신고의 운영 및 처리에 관한 법률",
  "source_type":"statute", "national_tier":1, "status":"active",
  "current_history":"현행", "effective_on":"20240703",
  "verification_status":"source-linked", "label":"LegalInstrument",
  "kind":"instrument", "instrument_kind":"법률", "src_id":"statute:257787" }

// Bill (19,847)
{ "id":"bill:PRC_N2W6S0L7W2J9D1I8A1B1I5I1G6H8F5",
  "name":"형사소송법 일부개정법률안(대안)(법제사법위원장)", "bill_no":"2220257",
  "age":22, "proc_dt":"2026-07-31", "proc_result_cd":"원안가결",
  "yes_tcnt":175, "no_tcnt":2, "blank_tcnt":1,
  "label":"Bill", "kind":"bill", "src_id":"PRC_…" }

// Legislator (320)
{ "id":"legislator:T2T8225E", "name":"강경숙", "district":"비례대표",
  "elect_type":"비례대표", "units":"제22대", "sex":"여",
  "label":"Legislator", "kind":"legislator",
  "current_party":"조국혁신당", "src_id":"T2T8225E" }

// Party (8)
{ "id":"party:조국혁신당", "name":"조국혁신당", "poly_cd":"101218",
  "label":"Party", "kind":"party", "src_id":"조국혁신당" }

// Category (16)
{ "id":"category:C-BIRTH", "name":"출산장려ㆍ양육지원", "level":1,
  "label":"Category", "kind":"category", "src_id":"C-BIRTH" }
```

> ⚠️ **id 접두 규칙에 주의.** Ordinance는 `"ordinance:" + "ordin:{mst}"` 로 **접두가 두 번** 붙는다(`ordinance:ordin:2146281`). LegalInstrument도 `"instrument:" + "statute:{mst}"`. 엣지의 `source`/`target` 도 같은 형식이므로 문자열 그대로 조인하면 되지만, 원본 ID(`ordin:2146281`)를 얻으려면 첫 접두만 벗겨야 한다.

```typescript
export type NodeLabel = "Region" | "Ordinance" | "LegalInstrument"
                      | "Bill" | "Legislator" | "Party" | "Category";

export interface GraphNodeBase {
  id: string; name: string; label: NodeLabel; kind: string; src_id: string;
}
export interface RegionNode extends GraphNodeBase {
  label: "Region"; full_name: string; level: 1|2|3|4;
  sig_cd: string; has_legislation: 0|1;
  status: "active"|"abolished"|"merged"|"renamed";
}
export interface OrdinanceNode extends GraphNodeBase {
  label: "Ordinance"; org_name: string | null; ord_kind: string;
  local_tier: "L1"|"L2"|null; delegation_type: string|null;
  department: string|null; enacted_on: string|null;   // "YYYYMMDD"
  effective_on: string|null; rr_cls_cd: string|null;
  article_count: number|null; status: "active"|"repealed"|"superseded";
  verification_status: "source-linked"|"article-verified"|"needs-review";
  region_id: string|null;
}
export interface InstrumentNode extends GraphNodeBase {
  label: "LegalInstrument"; source_type: string; national_tier: number|null;
  status: string; current_history: string|null; effective_on: string|null;
  verification_status: string; instrument_kind: string;
}
export interface BillNode extends GraphNodeBase {
  label: "Bill"; bill_no: string; age: number; proc_dt: string|null;
  proc_result_cd: string|null;
  yes_tcnt: number|null; no_tcnt: number|null; blank_tcnt: number|null;
}
export interface LegislatorNode extends GraphNodeBase {
  label: "Legislator"; district: string|null; elect_type: string|null;
  units: string|null; sex: string|null; current_party: string|null;
}
export interface PartyNode extends GraphNodeBase { label:"Party"; poly_cd: string|null; }
export interface CategoryNode extends GraphNodeBase { label:"Category"; level: number; }

export type GraphNode = RegionNode|OrdinanceNode|InstrumentNode|BillNode
                      |LegislatorNode|PartyNode|CategoryNode;
export interface NodesFile { as_of_date:string; stale:boolean; count:number; nodes:GraphNode[]; }
```

---

### 2.5 `graph/edges.json` — 그래프 엣지 (1,125,036개 / 209MiB)

최상위: `{ as_of_date, stale, count, edges: [...] }`
공통 필드: `source`, `target`, `relation`. 나머지는 relation별 속성.

**relation 실측 분포(전수 스캔)와 속성 스키마**

| relation | 건수 | source → target | 추가 속성 |
|---|---:|---|---|
| `IN_CATEGORY` | 334,831 | Ordinance → Category | `confidence`(0~1), `method`(`"rule"`) |
| `DELEGATED_FROM` | 266,171 | Ordinance → LegalInstrument | `delegation_type`, `source_path`, `inferred`(0/1) |
| `PROPOSED_BY` | 241,546 | Bill → Legislator | `role`(`"RST"` 대표 / `"PUBL"` 공동) |
| `HAS_ORDINANCE` | 199,858 | Region → Ordinance | `ord_kind`(`"조례"`/`"규칙"` 등) |
| `VOTED` | 57,178 | Legislator → Bill | `result_vote_mod`, `party_at_vote`, `vote_date` |
| `SIMILAR_TO` | 21,740 | Ordinance → Ordinance | `rank`, `model_name`, `weight`, `cosine_sim` |
| `CITES` | 1,751 | Ordinance → LegalInstrument | `citation_type`(`"reference"` 등) |
| `ADJACENT_TO` | 1,310 | Region → Region | `contiguity_type`(`"queen"`), `same_province`(0/1). V-World 경계 기준 shapely 재계산(2026-09-01). 그래프 번들은 2026-08-21 빌드값 1,098 |
| `CONTAINS` | 529 | Region → Region | — (광역→기초 포함위계) |
| `MEMBER_OF` | 299 | Legislator → Party | — |
| `AMENDED_BY` | 18 | Ordinance → Ordinance | — |
| `SUCCEEDED_BY` | 17 | Region → Region | `succession_type`(`"통합"`), `effective_date` |

> `graph-stats.json` 의 `edge_counts` 는 `DELEGATED_FROM/SUBORDINATE_TO` 를 307,779 로 합산 표기하지만, **실제 edges.json 안의 `relation` 값 기준으로는 `DELEGATED_FROM` 266,171건이다.** 화면 수치는 실제 파일 기준을 써라. `FUNDED_BY`·`ENACTS` 엣지는 **0건(파일에 없음)**.

실측 샘플:
```jsonc
{"source":"region:11","target":"ordinance:ordin:2023991","relation":"HAS_ORDINANCE","ord_kind":"조례"}
{"source":"region:11","target":"region:11110","relation":"CONTAINS"}
{"source":"region:11110","target":"region:11380","relation":"ADJACENT_TO","contiguity_type":"queen","same_province":1}
{"source":"region:29","target":"region:12","relation":"SUCCEEDED_BY","succession_type":"통합","effective_date":"2026-07-01"}
{"source":"ordinance:ordin:2146281","target":"instrument:statute:259255","relation":"DELEGATED_FROM","delegation_type":"law-delegated","source_path":"citation-backfill","inferred":1}
{"source":"ordinance:ordin:2146281","target":"instrument:statute:259255","relation":"CITES","citation_type":"reference"}
{"source":"ordinance:ordin:2146281","target":"ordinance:ordin:2006769","relation":"SIMILAR_TO","rank":1,"model_name":"char-ngram-tf","weight":0.742416,"cosine_sim":0.742416}
{"source":"ordinance:ordin:2146281","target":"category:C04","relation":"IN_CATEGORY","confidence":0.7143,"method":"rule"}
{"source":"bill:PRC_K2L5…","target":"legislator:M4O5221T","relation":"PROPOSED_BY","role":"RST"}
{"source":"legislator:T2T8225E","target":"bill:PRC_N2W6…","relation":"VOTED","result_vote_mod":"찬성","party_at_vote":"조국혁신당","vote_date":"20260731 163334"}
{"source":"legislator:T2T8225E","target":"party:조국혁신당","relation":"MEMBER_OF"}
{"source":"ordinance:ordin:789574","target":"ordinance:ordin:2014567","relation":"AMENDED_BY"}
```

```typescript
export type EdgeRelation =
  | "HAS_ORDINANCE" | "DELEGATED_FROM" | "CITES" | "IN_CATEGORY"
  | "SIMILAR_TO" | "ADJACENT_TO" | "CONTAINS" | "SUCCEEDED_BY"
  | "AMENDED_BY" | "PROPOSED_BY" | "VOTED" | "MEMBER_OF";

export interface GraphEdge {
  source: string; target: string; relation: EdgeRelation;
  // relation별 선택 속성 (없으면 undefined)
  ord_kind?: string;                                   // HAS_ORDINANCE
  delegation_type?: string; source_path?: string; inferred?: 0|1;  // DELEGATED_FROM
  citation_type?: string;                              // CITES
  confidence?: number; method?: string;                // IN_CATEGORY
  rank?: number; model_name?: string; weight?: number; cosine_sim?: number; // SIMILAR_TO
  contiguity_type?: string; same_province?: 0|1;       // ADJACENT_TO
  succession_type?: string; effective_date?: string;   // SUCCEEDED_BY
  role?: "RST" | "PUBL";                               // PROPOSED_BY
  result_vote_mod?: string; party_at_vote?: string; vote_date?: string; // VOTED
}
export interface EdgesFile { as_of_date:string; stale:boolean; count:number; edges:GraphEdge[]; }
```

---

### 2.6 `changes/*.json` · `state/watermarks.json` · `meta/graph-stats.json`

**`changes/latest.json`** (100KB, 최근 200건) / **`changes/feed-2026-08.json`** (11MB, 23,066건)
최상위 `{ as_of_date, stale, count, changes: [...] }`. `changes[]` 는 `regions/*.json` 의 `recent_changes` 보다 필드가 많다(12개):

| 필드 | 타입 | 예시 |
|---|---|---|
| `change_id` | string(32hex) | `"d32229e3bb5d42c2a797f21cd7fa6815"` |
| `ts` | string | `"2026-08-19T15:26:03+0900"` |
| `source` | string | `"budget"` |
| `scope` | string | `"laf:4900000\|fyr:2025"` |
| `entity_type` | string | `"budget"` |
| `entity_id` | string | `"laf:4900000\|fyr:2025"` |
| `entity_name` | string | `"4900000 2025 세출(5627건)"` |
| `event` | string | `"budget_changed"` |
| `before` | string | `""` |
| `after` | string | `"sha256:0595…"` |
| `region_code` | string\|null | `"50"` ← **sig_cd 5자리가 아닐 수 있다**(광역 2자리 등) |
| `official_url` | string\|null | `null` |

> ⚠️ `region_code` 는 값이 `"50"`(광역 2자리)·`"48890"`(5자리) 처럼 **자릿수가 섞여 있다.** 지역 필터에 쓸 때는 prefix 매칭으로 처리하라. DB 스키마의 `fields_changed` 필드는 **관측 샘플에 나타나지 않았다(확인 필요)**.

**`state/watermarks.json`** — `{ generatedAt, count:498, watermarks:[...] }`. 항목 12필드:
`source, scope, cursor, last_run, last_success, last_hash, status, rows_seen, changed, retry_count, run_id, note`.
(manifest 내장 요약본은 이 중 6필드만 담는다.)

**`meta/graph-stats.json`** — §2.1 `graph_stats` 와 동일 + `as_of_date`, `total_nodes`(250,416), `total_edges`(1,125,036).

```typescript
export interface ChangesFile { as_of_date:string; stale:boolean; count:number; changes:ChangeFull[]; }
export interface ChangeFull {
  change_id:string; ts:string; source:string; scope:string;
  entity_type:string; entity_id:string; entity_name:string; event:string;
  before:string; after:string;
  region_code:string|null; official_url:string|null;
}
export interface WatermarksFile { generatedAt:string; count:number; watermarks:Watermark[]; }
export interface Watermark extends WatermarkBrief {
  last_run:string|null; last_hash:string|null;
  rows_seen:number; retry_count:number; run_id:string|null; note:string|null;
}
```

---

### 2.7 ★ 지도 경계 — `reference/` (번들 밖이지만 프론트 필수)

**`viz/public/geo/municipalities.geojson`** (1.1MB) — 현행 지도 경계
- `type`: `"FeatureCollection"`, `features`: **227개**(기초자치단체 전수)
- 출처: 국토교통부 V-World `LT_C_ADSIGG_INFO`·`LT_C_ADSIDO_INFO`, EPSG:4326, 2026-09-01 실호출
- 일반구 41개는 상위 시 13곳으로 병합(dissolve). 표시용 단순화 tolerance 0.002도

**`skorea-municipalities-2018-geo.json`** (18MB) — 크로스워크 교차검증용
- `type`: `"FeatureCollection"`, `features`: **250개**
- `crs`: `urn:ogc:def:crs:OGC:1.3:CRS84` (= EPSG:4326 경위도)
- `features[].geometry.type`: `"MultiPolygon"`
- `features[].properties`: `{ name, name_eng, base_year:"2018", code }`
- **`properties.code` 는 통계청/SGIS 시군구코드다.** 종로구 = `"11010"` ≠ 우리 `sig_cd` `"11110"`.

**`kostat_to_bjd.json`** (108KB) — 위 변환표. `{ _meta, mapping }`, `mapping` 은 250개 엔트리:

```jsonc
"11010": {
  "name":"종로구", "name_eng":"Jongno-gu", "sm_prefix":"11",
  "bjd_sig_cd":"11110",              // ← 이 값으로 regions/{sig_cd}.json 조인
  "bjd_sido_cd":"11", "bjd_sido_nm":"서울특별시", "bjd_name":"종로구",
  "match_method":"name+sido", "confidence":1.0, "verified":true,
  "note":null, "spine_status":"active"
}
```
`_meta.counts` 실측: `total:250, verified:250, duplicates:0, missing:0, reform_affected:33, spine_status:{active:247, abolished:3}`.

**프론트 조인 절차(권장)**
```ts
const xw = await fetch('/data/reference/kostat_to_bjd.json').then(r=>r.json());
const toSig = (kostatCode: string) => xw.mapping[kostatCode]?.bjd_sig_cd ?? null;
// GeoJSON feature → sig_cd → regions/{sig_cd}.json
```
> ⚠️ 250 폴리곤 vs 284 지역파일. 폴리곤은 **기초자치단체(시군구) 단위 2018년 경계**이고, 지역파일은 광역 16 + 기초 227 + 일반구 41 이다. 광역(16)은 하위 폴리곤을 dissolve 하거나 별도 광역 경계가 필요하다(**광역 경계 파일 미확보 — 확인 필요**). 일반구 41개는 폴리곤이 없다(부천 3구·화성 4구 등은 `_meta.caveats` 에 명시적으로 "(확인 필요)" 처리됨).
> DB `region_geometry` 테이블에도 250행만 있고 bbox·GeoJSON이 들어 있다(종로구 bbox `[126.9489, 37.5658, 127.0234, 37.6324]`, geojson 길이 7,622자). 필요하면 여기서 추출해 경량 토폴로지를 만들 수 있다.

---

### 2.8 파일 무결성 검증 시 주의 (실측 함정)

`manifest.files[].bytes` 와 **디스크 실제 크기가 다르다.**

| 파일 | manifest.bytes | 디스크 실제 | 차이 |
|---|---:|---:|---:|
| `graph/nodes.json` | 136,420,648 | 140,602,397 | +4,181,749 |
| `graph/edges.json` | 211,098,223 | 218,895,463 | +7,797,240 |

원인 확인: `nodes.json` 의 줄 수가 정확히 **4,181,749줄** — 즉 Windows 텍스트 모드로 기록되며 줄마다 `\r` 이 1바이트씩 추가됐다. manifest 의 `bytes`/`hash` 는 **LF 기준 논리 길이**다.
→ **sha256 검증을 하려면 `\r\n` → `\n` 정규화 후 해시하라.** 그냥 파일 바이트로 검증하면 290개 전부 불일치로 뜬다.

---

## 3. DB 테이블 요약

- `system/db/schema.sql` 이 선언하는 테이블 **30개** + 런타임 생성 **3개**(`node_embeddings`, `neural_similarity`, `region_features`) = **실제 DB 33개**.
- DB 파일: `system/data/policymap.db` **4.6GB** (WAL 모드, `-shm`/`-wal` 동반).
- 프론트는 이 DB를 직접 읽지 않는다. 아래 표는 **정적 번들에 없는 데이터가 어디 있는지** 찾기 위한 지도다.

| # | 테이블 | 행수 | 주요 컬럼 | 용도 / 프론트 관련성 |
|---:|---|---:|---|---|
| 1 | `instrument_kind` | 38 | kind, source_type, national_tier, local_tier, tier_disputed | 규범종류 통제어휘. 위계 색상 범례 |
| 2 | `categories` | 16 | code, name, level, parent_code, keywords | 정책분야 C01~C14 + C-BIRTH/C-PET |
| 3 | `regions` | 556 | region_id, sig_cd, name, full_name, level, parent_region, has_legislation, population, lofin_laf_cd, status | 지역 스파인. 번들 `regions/` 의 원천 |
| 4 | `region_geometry` | **250** | region_id, crs, geojson, bbox, ring_signature | 경계 폴리곤. 광역·일반구 없음 |
| 5 | `region_adjacency` | 1,098 | region_id, neighbor_id, contiguity_type, same_province | Queen 인접. Moran/LISA 가중행렬 |
| 6 | `region_succession` | 17 | old/new_region_id, succession_type, effective_date, legal_basis | 전남광주통합(2026-07-01) 등 |
| 7 | `legal_instrument` | 29,811 | instrument_id, kind, national_tier, mst, law_id, name, effective_on, status, official_url, work_uri, lifecycle | 국가법령·행정규칙 |
| 8 | `articles` | 86,745 | article_id, instrument_id, article_no, title, body | 법령 조문 |
| 9 | `ordinances` | **199,858** (현행 159,452 / 폐지 40,406 / 본문확보 172,060) | ordinance_id, mst, region_id, name, ord_kind, delegation_type, enacted_on, repealed_on, rr_cls_cd, status, verification_status, official_url | **핵심 엔터티** |
| 10 | `ordinance_articles` | 2,365,068 | oa_id, ordinance_id, article_no, title, body | 조례 조문(RAG 검색 대상) |
| 11 | `ordinance_appendix` | (확인 필요) | appendix_id, file_url, title | 별표·서식 링크 |
| 12 | `delegations` | 421,627 (해석완료 307,779 = 73.0%) | delegation_id, child_kind/id/article, parent_kind/id/article, relation, delegation_type, source_path, inferred | **법령 위계 그래프의 뼈대** |
| 13 | `instrument_relations` | 812,339 | rel_id, src/dst, relation(CITES/AMENDED_BY/…), citation_text, citation_type | 인용·개정·효력우선 |
| 14 | `parties` | 8 | party_id, name, poly_cd | 정당 |
| 15 | `legislators` | 320 | legislator_id(MONA_CD), name, current_party, district, elect_type | 국회의원 |
| 16 | `bills` | 19,847 | bill_id, bill_no, age, name, proc_result_cd, member/vote/yes/no/blank_tcnt | 의안 |
| 17 | `bill_proposers` | (확인 필요; 그래프 PROPOSED_BY 241,546) | bill_id, legislator_id, role(RST/PUBL) | 발의자 |
| 18 | `votes` | 57,178 | bill_id, legislator_id, result_vote_mod, party_at_vote, vote_date | **의원별 표결** |
| 19 | `budget_lines` | 933,527 | budget_id, fyr, laf_cd, region_id, dbiz_nm, field, sector, budget_now, alloc_amt, exe_amt, exe_ymd | 세부사업별 세출 |
| 20 | `ordinance_budget_link` | **183,145** (verified=1 **421** / verified=0 182,655 / -1 69 · conf≥0.8 **42,872**) | ordinance_id, budget_id, match_method, confidence, verified | **확률적 매칭. §6 표기규율 필수** |
| 21 | `ordinance_category` | 조례 187,983건(94.1%) 분류 / 행 329,026 | ordinance_id, category_code, confidence, method | 분야 분류 |
| 22 | `embeddings` | (확인 필요) | item_id, item_type, model_name, dim, vector, norm | 텍스트 임베딩(char-ngram-tf) |
| 23 | `similarity_edges` | **21,740** | src_id, dst_id, cosine_sim, rank, model_name | 통계 유사 조례 kNN |
| 24 | `verification` | 1,205 | entity_type/id, status, citation/article 검증 카운터 | 3단계 검증 |
| 25 | `watermarks` | 498 | source, scope, cursor, last_success, status, changed | 증분 수집 상태(전부 ok) |
| 26 | `change_log` | 23,066 | change_id, ts, entity_type/id/name, event, region_code, official_url | 변경 피드 |
| 27 | `code_crosswalk` | **0** | sig_cd, vworld/law_org/lofin 코드 | 비어 있음 → `reference/kostat_to_bjd.json` 사용 |
| 28 | `schema_meta` | 3 | key, value | schema_version=1 |
| 29 | `ordinance_work` | 159,434 | work_id, author_token, name_norm, expression_count, lifecycle | FRBR Work 축(조례 개정판 묶기) |
| 30 | `temporal_audit` | 7,060 | audit_id, rule(T1~T8), severity, observed, repaired | 시간 정합성 감사 |
| +1 | `node_embeddings` | **661,297** (graphsage-numpy 242,891 / metapath2vec-numpy 209,203 / node2vec-numpy 209,203) | node_id, model_name, node_kind, dim, vector, norm | `similar_ordinances`·`neural_similar_regions` 의 `model` 인자 허용값이 곧 이 3종 |
| +2 | `neural_similarity` | (확인 필요) | src_id, dst_id, model_name, cosine_sim, rank, node_kind | 신경망 유사도 Top-k |
| +3 | `region_features` | 227 | region_id, rtype, population, area_km2, pop_density, budget_total, fiscal_self_ratio, welfare_ratio, ordinance_count, fyr | **유사 지자체 계산용 물화 테이블** |

> 카테고리 행 수는 파이프라인 로그 329,026행 vs 그래프 `IN_CATEGORY` 334,831 엣지로 불일치한다. 그래프가 이후에 재생성됐다. 화면 표기는 `graph-stats.json` 기준을 쓰라.

---

## 4. MCP tool 14종 API 명세

- 위치: `system/policymap/mcp_server/server.py` (2,518줄)
- 프로토콜: MCP stdio, `protocolVersion "2024-11-05"` · 서버명 `policymap-ordinance-graph` v0.1.0
- **모든 tool은 읽기 전용.** 응답은 예외 없이 아래 봉투로 감싸진다.

```typescript
export interface McpEnvelope<T> {
  data: T;
  as_of_date: string;
  stale: boolean;
  execution_allowed: false;      // 항상 false — 실행계 동작 금지
  disclaimer: string;            // "이 응답은 의사결정 지원을 위한 참고 정보이며 법률 판단·유권해석이 아닙니다. 근거 조문과 law.go.kr 원문을 직접 확인하십시오."
  warning?: string;              // stale=true 일 때만
  freshness?: { as_of_date:string; age_days:number; stale:boolean; max_age_days:number };
  official_url?: string;         // 단건 조회 tool에서만
}
```
**UI는 `disclaimer` 를 반드시 렌더링해야 한다.** 접거나 생략하면 심사 정확성 항목에서 감점 소지.

또한 대부분의 payload에 `_engine` 필드가 붙는다(`"analytics.peers.recommend_ordinances"`, `"fallback-sql"` 등). `_engine` 이 `fallback-*` 이면 **강등된 결과**이므로 화면에 "간이 계산" 배지를 붙여라.

리소스 3종도 제공: `ordinance-graph://status`(신선도·규모), `ordinance-graph://peers/{sig_cd}`, `ordinance-graph://changes?since={since}`.

### 4.1 tool 목록

| # | 이름 | 입력(필수 굵게) | 출력 핵심 필드 |
|---:|---|---|---|
| 1 | `search_ordinance` | query, region_id, sig_cd, ord_kind, status(기본 `active`), limit(기본30/최대200) | `{query, count, limit, results[]}` |
| 2 | `get_ordinance` | ordinance_id, mst, include_articles(기본 true) | `{ordinance, region, history, article_count, articles[], legal_basis[], citations[], verification_status}` |
| 3 | `similar_regions` | sig_cd, region_id, k(기본10), features[] | `{target, peers[], method}` |
| 4 | `gap_analysis` | region_id, sig_cd, parent_instrument_id, region_level(기본2) | `{region_id, gap_count, mandatory_unmet, gaps[]}` 또는 커버리지 매트릭스 |
| 5 | `diffusion_timeline` | **template**, since | `{template, adopter_count, first_adopter, latest_adopter, timeline[]}` |
| 6 | `region_profile` | region_id, sig_cd | `{region, ordinance_count, ordinance_by_kind[], budget_latest, neighbors[], succession[], recent_changes[]}` |
| 7 | `bill_vote_breakdown` | bill_id, bill_no | `{bill, tally_reported, tally_from_votes, yes_ratio, party_breakdown[], proposers[]}` |
| 8 | `semantic_search_ordinance` | **query**, k(기본10/최대50), scope(기본`all`), hops(0~2,기본1), use_graph, graph_weight(기본0.5), with_text | `{query,k,scope,hops,count,results[],verification_summary,_engine}` |
| 9 | `similar_ordinances` | ordinance_id, mst, k, model(기본`graphsage-numpy`) | `{ordinance, k, model, count, similar[], verification_summary, interpretation}` |
| 10 | `neural_similar_regions` | region_id, sig_cd, k, model | `{base_region, k, model, peers[], similarity_basis:"graph-embedding", verification_status:"unverified", contrast}` |
| 11 | `ordinance_effectiveness` | ordinance_id, mst, region_id, sig_cd, fyr, min_confidence(기본0.0) | `{link_count, budget_lines, totals{…exec_rate_vs_now,exec_rate_vs_alloc}, by_fiscal_year[], by_ordinance[], verification{verified_links,auto_links,status}}` |
| 12 | `explain_path` | **from_id**, **to_id**, max_hops(1~6,기본4) | `{from,to,found,hops,path[],path_string,relations[],shared_context,explanation,verification_status}` |
| 13 | `recommend_ordinances` ★킬러 | sig_cd, region_id, k(기본15), min_peers(기본3), limit(기본30/최대200) | `{target, peers[], peer_pool_size, my_policy_count, recommendations[], method, verification_status:"unverified", caveat}` |
| 14 | `spatial_autocorrelation` | **metric**, fyr(기본2025), permutations(기본999), lisa(기본true) | `{metric, n, moran_i, expected_i, p_sim, z_sim, interpretation, lisa[], lisa_summary}` |

> tool 13·14 는 **폴백이 없다.** `policymap.analytics`(numpy 필요) 미탑재 시 `ToolError` 를 던진다. 나머지는 엔진 부재 시 SQL 폴백으로 강등되며 `_engine` 에 표시된다.

### 4.2 주요 출력 스키마 (실코드 기준)

**#13 `recommend_ordinances` — 격차분석 킬러 기능**
```typescript
interface Recommendation {
  policy_key: string;             // 정규화된 정책명 (지자체명·법형식 접미 제거)
  peer_count: number;             // 이 조례를 보유한 peer 수
  peer_share: number;             // peer_count / peer_pool_size
  repealed_peer_count: number;    // ★ 폐지한 peer 수
  repealed_peers: { region_id:string; name:string; repealed_on:string }[];  // 최대 3
  caution: string | null;         // ★ 폐지 사례 있으면 경고문, 없으면 null
  likely_variant_of_mine: boolean;// 우리 조례의 표기변이 가능성
  closest_own: { policy_key:string; similarity:number } | null;
  peers: { region_id:string; name:string; ordinance_id:string;
           ordinance_name:string; enacted_on:string; url:string }[];  // 최대 5
}
interface RecommendData {
  target: { region_id:string; sig_cd:string; name:string; rtype:string;
            indicators: Record<string, number|null> };
  peers: { region_id:string; name:string }[];
  peer_pool_size: number;
  my_policy_count: number;
  suppressed_exact_duplicate: number;
  flagged_as_variant: number;
  recommendations: Recommendation[];
  method: { partition_by_type:boolean; candidate_pool:number;
            weights: Record<string,number>;
            weight_provenance: Record<string,"mois_public"|"ours">;
            policy_profile_weight:number; min_indicator_coverage:number;
            missing_mois_indicators:string[];
            min_peers:number; same_sido_boost:number;
            dup_threshold:number; exclude_variants:boolean; policy_key:string };
  verification_status: "unverified";
  caveat: string;
}
```
정렬: `likely_variant_of_mine` 오름차순 → `peer_count` 내림차순 → `policy_key`.
**UI는 `caution ≠ null` 인 항목에 반드시 폐지 경고 배지를 달아야 한다.**

**#3 `similar_regions` — 유사 지자체 (행안부 기준)**
```typescript
interface Peer {
  region_id:string; sig_cd:string; name:string; rtype:string; // 자치구/시/군
  similarity:number; weighted_distance:number;
  policy_profile_cosine:number|null;
  indicator_gap_sd: Record<string, number>;   // 지표별 z-거리
  indicators: { population:number|null; area_km2:number|null;
                fiscal_self_ratio:number|null; welfare_ratio:number|null;
                budget_total:number|null };
}
```
가중치 실측(`analytics/peers.py::DEFAULT_WEIGHTS`)과 **출처 표기**:

| 지표 | 가중치 | provenance |
|---|---:|---|
| `fiscal_self_ratio` 자체재원비율 | 0.30 | `mois_public` (지방재정365 공개 설명) |
| `population` 인구 | 0.25 | **`ours`** (행안부 원 가중치 미확인) |
| `budget_total` 예산총액 | 0.25 | **`ours`** |
| `area_km2` 면적 | 0.13 | **`ours`** |
| `welfare_ratio` 사회복지비 비중 | 0.07 | `mois_public` |

미확보 행안부 지표: 인구증감률·고령인구비율·조출생률 (`MISSING_MOIS_INDICATORS`).
→ **발표·UI에서 "행안부 기준"이라고 단정하지 말고 `weight_provenance` 를 그대로 노출하라.** `ours` 4개 중 3개가 우리가 배분한 값이다.
level=3(일반구)은 가드에 걸려 `{peers:[], reason:"..."}` 를 반환한다.

**#14 `spatial_autocorrelation` — Moran's I / LISA**
```typescript
interface MoranData {
  metric:string; level:number; weights:"row";
  n:number; universe:number;
  excluded:{region_id:string;name:string;reason:string}[]; n_excluded:number;
  moran_i:number|null; expected_i:number; mean:number; sd:number; s0:number;
  p_sim:number; z_sim:number|null; permutations:number;
  interpretation:string;
  lisa?: LisaRow[];
  lisa_summary?: {
    fdr_alpha:number; n_significant_raw_p05:number; n_significant_fdr:number;
    expected_false_positives_without_fdr:number;
    by_quadrant: Record<"HH"|"LL"|"HL"|"LH", number>;
    clusters: Record<"HH"|"LL"|"HL"|"LH", string[]>;
  };
  reading_guide:string;
}
interface LisaRow {
  region_id:string; sig_cd:string; name:string;
  value:number; z_score:number; spatial_lag_z:number; local_i:number;
  quadrant:"HH"|"LL"|"HL"|"LH";
  p_sim:number; q_value:number;
  significant:boolean;      // ★ BH-FDR 통과 여부
  n_neighbors:number;
}
```
`metric` 허용값: `ordinance_count` | `budget_per_capita` | `population` | `welfare_ratio` | `fiscal_self_ratio` | `pop_density` | `template:<키워드>` | `adoption_year:<키워드>` | `adoption_year_resid:<키워드>`.
**지도에 색을 칠할 때는 `significant === true` 인 행만 칠하라** (미보정 시 위양성 10곳 안팎 발생, `reading_guide` 에 명시).

**#11 `ordinance_effectiveness`**
```typescript
interface EffectivenessData {
  link_count:number; budget_lines:number;
  fyr_filter:number|null; min_confidence:number;
  totals:{ alloc_amt:number; budget_now:number; exe_amt:number;
           exec_rate_vs_now:number|null; exec_rate_vs_alloc:number|null };
  by_fiscal_year:{ fyr:number; lines:number; alloc_amt:number; budget_now:number;
                   exe_amt:number; exe_ymd:string|null;
                   exec_rate_vs_now:number|null; exec_rate_vs_alloc:number|null }[];
  by_ordinance:{ ordinance_id:string; name:string; region_id:string;
                 official_url:string|null; verification_status:string; status:string;
                 lines:number; alloc_amt:number; budget_now:number; exe_amt:number;
                 exec_rate_vs_now:number|null; exec_rate_vs_alloc:number|null;
                 verified_links:number; auto_links:number;      // ★
                 methods:Record<string,number>;
                 programs:{ budget_id:string; fyr:number; dbiz_nm:string; field:string|null;
                            alloc_amt:number; budget_now:number; exe_amt:number;
                            exec_rate:number|null;
                            confidence:number; match_method:string; verified:boolean }[] }[]; // 최대 20
  verification:{ verified_links:number; auto_links:number; status:string };
}
```
집행률 정의: `exec_rate_vs_now = exe_amt / budget_now`, `exec_rate_vs_alloc = exe_amt / alloc_amt`.

**#7 `bill_vote_breakdown`**
```typescript
interface VoteData {
  bill:{ bill_id:string; bill_no:string; age:number; name:string; committee:string|null;
         propose_dt:string|null; proc_dt:string|null;
         proc_result:string|null; proc_result_cd:string|null };
  tally_reported:{ member_tcnt:number|null; vote_tcnt:number|null;
                   yes_tcnt:number|null; no_tcnt:number|null; blank_tcnt:number|null };
  tally_from_votes:{ "찬성":number; "반대":number; "기권":number; "기타":number; "합계":number };
  yes_ratio:number|null;
  party_breakdown:{ party:string; "찬성":number; "반대":number; "기권":number;
                    "기타":number; "합계":number; "찬성률":number }[];  // 합계 내림차순
  proposers:{ role:"RST"|"PUBL"; legislator_id:string; name:string;
              current_party:string|null; district:string|null }[];
}
```
> `tally_reported`(원천 집계) 와 `tally_from_votes`(의원별 표결 재집계) **두 값이 다를 수 있다.** 둘 다 보여주고 불일치 시 표기하라 — 이것이 데이터 정직성의 근거가 된다.

**#8 `semantic_search_ordinance` 결과 항목**
```typescript
interface SearchHit {
  id:string; name:string;
  node_type:"ordinance"|"instrument";
  region_id:string|null; org_name:string|null; official_url:string|null;
  doc_key:string; article_no:string|null; article_title:string|null;
  doc_kind:"ordinance_article"|"statute_article";
  search_rank:number; search_score:number;
  bm25_rank:number|null; dense_rank:number|null;
  graph_rank:number|null; via:string|null; path:unknown|null; evidence:unknown|null;
  origin:"search"|"graph";           // ★ graph 확장으로 들어온 항목 구분
  verification_status:string; verified:boolean; status:string;
  text?:string;                      // with_text=true
}
```
응답에 `verification_summary:{by_status, verified, unverified}` 가 함께 온다.

**#12 `explain_path` 스텝**
```typescript
interface PathStep {
  hop:number; relation:EdgeRelation; from_label:string; to_label:string;
  inferred:boolean;
  evidence?:{ citation_text?:string; parent_article?:string;
              cosine_sim?:number; confidence?:number };
}
// verification_status: 스텝 중 inferred 가 하나라도 있으면 "unverified", 아니면 "verified"
// shared_context: shared_parent_instruments[], shared_categories[],
//                 direct_similarity{cosine_sim,model_name,basis:"statistical(char-ngram-tf)"},
//                 direct_neural_similarity{...,basis:"graph-embedding"},
//                 same_region | adjacent_regions
```
경로 미발견 시 `found:false` 이며, 설명문에 **"상한 내에서 찾지 못함"** 이라고 나온다. UI가 이를 "연결 없음"으로 단정하면 안 된다.

---

## 5. 화면별 필요 데이터 매핑

| # | 화면 | 필요 파일 / 필드 | MCP tool(동적) | 상태 |
|---:|---|---|---|---|
| 1 | **지도 코로플레스** | `reference/skorea-municipalities-2018-geo.json`(250 폴리곤) + `reference/kostat_to_bjd.json`(`mapping[code].bjd_sig_cd`) + `regions/index.json`(`ordinance_total`) + `regions/{sig_cd}.json`(`top_categories[].count`, `budget.exe_amt`/`budget_now`, `population`) | `spatial_autocorrelation`(Moran/LISA 레이어) | ✅ 정적만으로 가능. 단 LISA는 tool 필요 |
| 2 | **유사 지자체 추천** | (정적 없음) | `similar_regions` — `peers[].similarity`, `indicator_gap_sd`, `method.weight_provenance` | ⚠️ **정적 번들에 없음.** 사전계산 JSON 생성 필요 |
| 3 | **격차분석(킬러)** | (정적 없음) | `recommend_ordinances` — `recommendations[].{policy_key, peer_count, peer_share, repealed_peer_count, caution, likely_variant_of_mine, closest_own, peers[]}` | ⚠️ **정적 번들에 없음.** 시연용 사전계산 필수 |
| 4 | **법령 위계 그래프** | `graph/edges.json` relation=`DELEGATED_FROM`(266,171) / `CITES`(1,751) + `graph/nodes.json` label=`Ordinance`,`LegalInstrument`(`national_tier` 로 층 배치) | `get_ordinance.legal_basis[]`, `explain_path` | ⚠️ 용량 §7. CITES는 810,150건이 잘려 그래프로는 부분만 표현 |
| 5 | **정책 생애주기 타임라인** | `graph/nodes.json` Ordinance(`enacted_on`,`effective_on`,`rr_cls_cd`,`status`) + `AMENDED_BY`(18) 엣지 | `diffusion_timeline` — `timeline[]` | ✅ 노드 속성만으로 가능(폐지 40,406건 포함) |
| 6 | **조례↔예산 실효성** | `regions/{sig_cd}.json`.`budget`(지자체 총량만) | `ordinance_effectiveness` — `by_ordinance[].programs[].{confidence,verified,match_method}` | ❌ **조례 단위 링크는 정적 번들에 전혀 없다**(FUNDED_BY 183,145 전량 skip). tool 또는 사전계산 필수 |
| 7 | **국회 표결** | `graph/nodes.json` Bill(`yes_tcnt`,`no_tcnt`,`blank_tcnt`,`proc_result_cd`) + `VOTED`(57,178, `result_vote_mod`·`party_at_vote`) + `PROPOSED_BY`(241,546) + Party(8)·Legislator(320) 노드 | `bill_vote_breakdown` | ✅ 그래프 번들만으로 정당별 집계 가능 |
| 8 | **RAG 조문 검색** | (정적 없음 — 인덱스 9.5GB는 서버측) | `semantic_search_ordinance` | ❌ 서버 필요. 정적 데모는 결과 캐시 JSON으로 |
| 9 | **신경망 유사 조례/지자체** | `graph/edges.json` relation=`SIMILAR_TO`(21,740, `cosine_sim`) — 이건 **통계 임베딩(char-ngram-tf)** 이다 | `similar_ordinances`, `neural_similar_regions` (graphsage-numpy 등) | ⚠️ 정적에 있는 건 통계 유사도. 신경망(661,297 임베딩)은 tool 필요 |
| 부가 | **변경 피드 / 신선도 배너** | `changes/latest.json`(200건), `changes/feed-2026-08.json`(23,066), `manifest.{as_of_date,stale,stale_days}`, `state/watermarks.json` | — | ✅ |
| 부가 | **인접 지자체 / 승계** | `graph/edges.json` `ADJACENT_TO`(1,098), `CONTAINS`(529), `SUCCEEDED_BY`(17) | `region_profile.neighbors[]` | ✅ 전체가 20KB 미만 — 별도 shard 권장 |

> **정리: 정적 번들만으로 완결되는 화면은 1·5·7·부가 뿐이다.** 2·3·6·8·9 는 MCP tool 결과를 미리 뽑아 JSON으로 저장해야 오프라인 데모가 된다. `viz/public/data/api/{peers,gap,effectiveness,diffusion,search,votes}.json` 이 이미 그 자리를 잡아두었다 — 그 파일들을 실 tool 출력으로 채우는 것이 다음 작업이다.

---

## 6. UI가 반드시 지킬 표기 규율

심사의 "정확성" 항목과 직결된다. 아래는 권고가 아니라 **필수**다.

### 6.1 조례↔예산 링크 — 확률적 매칭임을 숨기지 말 것
`ordinance_budget_link` 는 이름 유사도 기반 자동매칭이다. **전체 183,145건 중 `verified=1` 은 단 421건(0.23%)**.

표본 584건 검증 결과(`17_링크_표본검증_보고서.md`, 검증 시점 모집단 93,964건 기준):

| 구간 | 정밀도(엄격) | 95% CI |
|---|---:|---|
| 전체 | **64.9%** | 60.4~69.5 |
| 전체(관대: 명백한 오답만 실패) | 80.8% | 76.7~84.9 |
| `confidence ≥ 0.8` | **93.2%** | 89.8~96.6 |

> ⚠️ 이 정밀도는 링크 93,964건 시점의 값이다. **현재 모집단은 183,145건으로 늘었고 재검증하지 않았다.** 발표 시 "전체 정밀도 64.9%"는 검증 시점 값임을 명시하라.

**서비스 노출 등급(보고서 §4.3 권고안 그대로 구현할 것)**

| 등급 | 조건 | UI 문구 | 처리 |
|---|---|---|---|
| A. 근거 조례 | `conf ≥ 0.8` AND method ∈ {`name+article(+dept)`, `name+domain(+dept)`} | **"이 사업의 근거 조례로 추정"** | 기본 노출 |
| B. 관련 조례 | `conf ≥ 0.8` AND method ∈ `article-evidence` 계열 | **"관련 조례(직접 근거가 아닐 수 있음)"** | 별도 섹션 |
| C. 참고 | `0.7 ≤ conf < 0.8` | — | **접힘 처리**, 클릭 시 노출 |
| X. 비노출 | `conf < 0.7` 또는 method = `domain-topic` | — | **검색 인덱스에서 제외** |

- `verified === true` 인 421건만 **"확인됨"** 배지. 나머지는 전부 **"추정 연결"** 배지.
- `confidence` 는 등급(A/B/C)으로 보여주고, 원값이 필요하면 툴팁에.
- `match_method` 도 함께 노출(어떤 근거로 이어졌는지가 신뢰의 핵심).

### 6.2 폐지 조례를 선례로 추천하지 말 것
- 조례 목록·추천·검색 기본 필터: `status === "active"`. (`ordinances` 199,858 중 **40,406건이 폐지**다 — 필터를 안 걸면 5건 중 1건이 폐지 조례다.)
- `recommend_ordinances` 결과에서 `repealed_peer_count > 0` 이면 **폐지 경고 배지 + `caution` 문구**를 그대로 출력:
  > "유사 지자체 중 폐지 사례 있음 — 상위법 개정 등으로 대체되었을 수 있으니 제정 전 확인 필요"
- `repealed_peers[].repealed_on` 을 함께 보여줄 것.
- `likely_variant_of_mine === true` 인 추천은 **하단 접힘**으로 내리고 `closest_own.policy_key` 를 병기("우리 「○○ 조례」와 유사할 수 있음, 유사도 0.87").

### 6.3 데이터 기준일·신선도
- **모든 화면 하단(또는 헤더)에 `as_of_date` 상시 노출.**
- `stale === true` → 상단 경고 배너: 서버 문구를 그대로 쓴다.
  > "데이터 기준일이 신선도 임계값을 초과했습니다(stale). 판단성 결과는 보수적으로 해석하고 최신 원문을 확인하십시오."
- `manifest.as_of_date`(2026-08-21)와 `regions/{sig_cd}.as_of_date`(2026-08-19)가 **다를 수 있다.** 지역 카드에는 지역 파일의 값을 쓸 것.
- `state/watermarks.json` 에서 `status !== "ok"` 인 파티션이 생기면 해당 지역/소스에 "수집 지연" 표시. (현재는 498/498 ok)

### 6.4 검증 상태 · 추정치
- `verification_status` 3단계를 그대로 표기: `article-verified` > `source-linked` > `needs-review`.
- 신경망/통계 유사도(`SIMILAR_TO`, `similar_ordinances`, `neural_similar_regions`)는 **전부 `unverified` 파생 추정치**다. "법적 동등성이 아님" 문구 필수.
- `DELEGATED_FROM` 엣지의 `inferred === 1` 은 **추론된 위임관계**다. 실선/점선으로 구분해 그려라. (위임 421,627건 중 해석 완료 307,779건 = 73.0%, 나머지는 미해석)
- `explain_path.verification_status === "unverified"` 면 경로 설명에 "추정 경로" 표시.

### 6.5 GraphRAG 배치
그래프 확장은 검색 랭킹 개선에 무익함이 실증됐다. **검색 결과 목록에 섞지 말고**, `origin === "graph"` 인 항목은 "관련 조례 확장" 별도 패널로 분리하라.

### 6.6 기타
- `execution_allowed: false` — 어떤 화면도 "이 조례를 제정하시겠습니까" 같은 실행 유도를 하지 않는다.
- `disclaimer` 전문을 렌더링한다.
- `official_url` 에 API 키(`OC={LAW_OC}`)가 들어 있다 → **배포 전 제거/치환 필수**(§2.3).
- `similar_regions` 가중치는 5개 중 3개가 우리가 정한 값(`ours`)이다. "행안부 기준"으로 단정 표기하지 말고 `weight_provenance` 를 노출하라.
- `level === 3`(일반구·행정시 41개)은 자치입법권이 없다. 조례 0건이 정상이며 "데이터 없음"이 아니라 **"자치입법권 없음"** 으로 표기.

---

## 7. 용량 주의 — graph/*.json 을 브라우저에 그대로 던지지 말 것

### 7.1 문제
| 파일 | manifest bytes | 디스크 | 항목 수 | 브라우저 파싱 |
|---|---:|---:|---:|---|
| `graph/nodes.json` | 136,420,648 | **140,602,397 (134MiB)** | 250,416 | ❌ 파싱만 수 초~수십 초, 메모리 1GB+ |
| `graph/edges.json` | 211,098,223 | **218,895,463 (209MiB)** | 1,125,036 | ❌ 사실상 불가 |
| `changes/feed-2026-08.json` | 10,999,756 | 11,253,488 (11MiB) | 23,066 | ⚠️ 가능하나 무겁다 |
| `reference/skorea-...geo.json` | — | 18MB | 250 feature | ⚠️ 가능하나 단순화 권장 |

`indent=2` 로 pretty-print 되어 있어(노드 1개당 약 12줄) 실제 정보량 대비 3~4배 부풀어 있다.

### 7.2 대안 (권장 순서)

**(a) 관계별 분리 export — 가장 저렴하고 효과 큼**
```
graph/edges/adjacency.json    ADJACENT_TO 1,098 + CONTAINS 529 + SUCCEEDED_BY 17  → ~150KB
graph/edges/similar.json      SIMILAR_TO 21,740                                   → ~3MB
graph/edges/votes.json        VOTED 57,178 + PROPOSED_BY 241,546 + MEMBER_OF 299  → ~50MB(추가 분할)
graph/edges/category.json     IN_CATEGORY 334,831                                 → ~40MB(추가 분할)
graph/edges/delegation.json   DELEGATED_FROM 266,171 + CITES 1,751                → ~50MB(추가 분할)
```
화면 1·5·7·부가 는 이 중 일부만 있으면 된다.

**(b) 지역 shard — 화면 단위 로딩**
```
graph/by-region/{sig_cd}.json = {
  region, ordinances[](해당 지역 조례 노드 요약),
  delegations[](그 조례들의 상위법), neighbors[], categories[]
}
```
지역당 조례 평균 ≈ 199,858 / 284 ≈ 700건 → 파일당 **200~500KB** 예상. 284개 생성해도 총 100MB 내외이고 **한 번에 1개만 로드**한다. 화면 4(위계 그래프)와 화면 1(지도 클릭 상세)의 정답.

**(c) 필드 다이어트 + minify**
- `indent=2` → `separators=(',',':')` 로만 바꿔도 **60~70% 축소**.
- 그래프 렌더에 불필요한 필드 제거(`department`, `phone`, `rr_cls_cd`, `verification_status` 등은 상세 패널에서 개별 fetch).
- 노드를 배열-of-배열 + 헤더로 압축: `{"cols":["id","name","label",...], "rows":[[...],[...]]}` → 키 반복 제거로 추가 40~50% 축소.
- gzip/brotli 정적 압축(`.json.gz`) — JSON은 8~12배 압축된다. 209MB → 20MB 내외. **Nginx/CDN에서 `Content-Encoding` 만 붙이면 프론트 코드 변경 없음. 가장 비용 대비 효과가 크다.**

**(d) 그래프 렌더 상한**
- 화면에 동시에 그리는 노드는 **2,000개 이하**로 제한. 그 이상은 클러스터링(`index/communities/*.json` 에 이미 커뮤니티 탐지 결과가 있다 — 조례유사도 10개 / 지역인접 12개, 모듈러리티 0.465 / 0.451, 합계 115KB).
- 기본 뷰 = 시드 노드 + 2홉 이내, 나머지는 "더 보기".

**(e) 서버측 필터(최종형)**
MCP tool 또는 얇은 API 서버를 두고 그래프 질의를 서버에서 처리. 대회 데모 규모에서는 (a)+(b)+(c)로 충분하다.

### 7.3 즉시 적용 체크리스트
- [ ] `graph/nodes.json`·`edges.json` 을 **프론트 번들/저장소에 커밋하지 않는다** (`.gitignore` 확인)
- [ ] `regions/`(1.5MB) + `meta/`(796B) + `changes/latest.json`(100KB) + `reference/kostat_to_bjd.json`(108KB) = **약 1.7MB** 가 초기 로딩 예산
- [ ] GeoJSON 18MB → mapshaper 등으로 단순화(좌표 정밀도 5자리, `-simplify 5%`) 시 1~2MB
- [ ] 해시 검증 시 CRLF→LF 정규화 (§2.8)

---

## 부록 A. 확인하지 못한 항목 (확인 필요)

| 항목 | 상태 |
|---|---|
| `ordinance_appendix` / `bill_proposers` / `embeddings` / `neural_similarity` 행수 | DB 전체 스캔이 4.6GB·저속 볼륨에서 타임아웃. 미측정 |
| 광역(시·도) 경계 폴리곤 파일 | 미확보. 기초 250개 dissolve 필요 |
| 일반구(부천 3구·화성 4구 등) 폴리곤 | `kostat_to_bjd.json._meta.caveats` 자체가 "(확인 필요)" 로 명시 |
| `change_log.fields_changed` 필드 | DDL엔 있으나 export 샘플에서 미관측 |
| 교육청(level=4, 19개) 정적 파일 | export 대상에서 제외됨 |
| 링크 정밀도 재검증 | 표본검증(584건)은 링크 93,964건 시점. 현재 183,145건 미재검증 |
| `similar_regions`/`recommend_ordinances` 실행 예시 출력 | numpy 의존 analytics 미실행. 스키마는 소스코드에서 확정, 실측 응답 샘플은 없음 |

## 부록 B. 참조 소스 파일

| 내용 | 파일 |
|---|---|
| 정적 번들 생성기 | `F:/policy_maps/system/policymap/graph/export.py` |
| MCP tool 14종 | `F:/policy_maps/system/policymap/mcp_server/server.py` |
| 유사 지자체·추천 로직 | `F:/policy_maps/system/policymap/analytics/peers.py` |
| Moran/LISA | `F:/policy_maps/system/policymap/analytics/spatial.py` |
| RAG 하이브리드 검색 | `F:/policy_maps/system/policymap/rag/retrieve.py` |
| DB DDL | `F:/policy_maps/system/db/schema.sql` |
| 링크 표본검증 | `F:/policy_maps/17_링크_표본검증_보고서.md` |
| 데이터 계약 | `F:/policy_maps/system/CONTRACTS.md` |
| 운영 절차 | `F:/policy_maps/system/RUNBOOK.md` |
