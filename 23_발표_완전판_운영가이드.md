# 23. 발표용 완전판 운영 가이드

> 대상: 2026-10-22 발표평가 / 작성 2026-08-23 / 근거는 전부 이 저장소에서 실측한 값이다.

---

## 1. 결론부터 — 14.3GB 전부가 필요한 게 아니다

깃허브에서 빠진 14.3GB 중 **완전판 구동에 실제로 필요한 것은 `policymap.db` 하나(4.31GB)** 다.
나머지는 없어도 화면이 그대로 뜬다. `viz/serve_full.py` 가 여는 파일이 DB 하나뿐이기 때문이다.

| 미포함 자산 | 크기 | 완전판에 필요? | 실측 근거 |
|---|---:|:---:|---|
| `system/data/policymap.db` | 4.31GB | **필수** | serve_full.py 가 직접 여는 유일한 파일 |
| `system/data/index/` (RAG) | 9.5GB | 선택 | 없으면 폴백 검색으로 자동 전환(§5) |
| `system/data/graph/` | 343MB | 불필요 | 서브그래프를 DB 에서 즉석 생성한다 |
| `system/data/reference/` | 21MB | 불필요 | 경계·코드 참조. 화면은 `viz/public/geo/` 를 쓴다 |
| `system/.env` (키 7종) | 4KB | 불필요 | 수집이 끝나 API 호출이 없다. Gemini 챗봇만 필요(§6) |
| `external/` | 60MB | 불필요 | 외부 저장소 클론. 재클론 가능 |

**즉 전달물은 압축한 DB 파일 1개다.**

```
system/scratchpad/ship/policymap.db.gz     1.14GB  (원본 4.31GB → 26%)
```

---

## 2. 배포본이 목업을 보여주고 있었다 (2026-08-23 수정)

점검 중 발견해 고쳤다. **커밋 `1f8ae14`.**

`viz/public/js/config.js` 의 `DATA_BASE` 기본값이 `"./data"`(가상데이터)였다.
두 경로의 실체가 완전히 다르다.

| 경로 | 조례 | 지역 | 법령 |
|---|---:|---:|---:|
| `./data` (구 기본값) | 302 | 27 | 80 |
| `../../system/data` (현 기본값) | **199,858** | **556** | **29,811** |

증상: `#/graph` 가 `api/graph/index.json` 을 못 찾아 구 목업 번들로 폴백해
**가짜 조례명을 그리고 있었다.** 기본값을 실데이터로 바꿔 해결했다.
가상데이터로 보려면 `?src=mock` 을 붙인다.

> 주의: 이 상대경로는 **웹 루트가 저장소 루트**일 때만 잡힌다.
> `viz/public/` 에서 직접 서버를 띄우면 상위 디렉터리에 접근할 수 없어 404 가 난다.
> `serve_full.py` 와 Vercel 배포는 둘 다 저장소 루트를 서빙하므로 문제없다.

---

## 3. 발표장 인터넷이 끊겨도 되게 만들었다

같은 커밋에서 `leaflet`·`chart.js`·`vis-network` 를 `viz/public/vendor/` 에 내장했다(1.1MB).

- 전: 외부 CDN(unpkg·jsdelivr) 의존 → 인터넷이 없거나 사내망이 막으면
  **지도·차트·그래프가 전부 표/목록으로 떨어진다.**
- 후: 로컬을 먼저 시도하고 실패 시에만 원격으로 넘어간다.
  실측 — 16개 화면 순회에서 **원격 CDN 요청 0건**, 네 라이브러리 모두 로컬에서 로드.

남는 온라인 의존은 **위성 basemap 타일 한 가지뿐**이고, 이건 선택 레이어다.
기본 코로플레스 지도는 로컬 GeoJSON(`viz/public/geo/`)을 써서 오프라인에서 그대로 뜬다.

---

## 4. 친구(배포 담당)에게 요청할 것

친구는 **배포본만 담당**하면 된다. 완전판(DB)은 로컬 전용이라 친구에게 4.31GB 를 보낼 필요가 없다.

### 4-1. 그대로 복사해 보낼 메시지

```
깃허브 업데이트 하나 올렸어. pull(또는 Sync fork) 한 번만 해주면 돼.
커밋 1f8ae14 — 두 가지 고쳤어.

1) 사이트 기본 데이터가 목업이었어.
   config.js 의 DATA_BASE 가 "./data" 였는데 그게 조례 302건짜리 표본이야.
   실데이터(system/data, 조례 199,858건)로 기본값을 바꿨어.
   그래프 화면이 목업 조례명 그리던 것도 이걸로 같이 해결됐어.
   목업으로 보고 싶으면 URL 에 ?src=mock 붙이면 돼.

2) leaflet / chart.js / vis-network 를 viz/public/vendor/ 에 내장했어(1.1MB).
   전에는 unpkg·jsdelivr 에서 받았는데, 발표장 인터넷이 막히면
   지도·차트·그래프가 전부 표로 떨어져서. 로컬 우선 → 실패 시 CDN 순서야.

배포 후에 이것만 확인해줘:
 - 첫 화면 '자치법규' 숫자가 199,858 로 나오는지 (302 면 아직 목업)
 - #/graph 상단에 "shard 색인 api/graph/index.json" 이라고 뜨는지
 - 개발자도구 Network 에 unpkg/jsdelivr 요청이 없는지

참고로 데이터 shard 는 .json.gz 로 사전압축돼 있어서
브라우저 DecompressionStream 을 써. 크롬·엣지·사파리 최신은 다 되는데
아주 옛날 브라우저면 안 뜰 수 있어.
```

### 4-2. 친구가 완전판까지 보고 싶다고 하면

그때만 `policymap.db.gz`(1.14GB) 를 구글드라이브로 전달하고, 아래를 알려준다.

```
1. 압축 풀어서 system/data/policymap.db 에 놓기 (4.31GB)
2. 저장소 루트에서  run_full.bat
3. http://127.0.0.1:8743/viz/public/index.html?full=1
API 키는 필요 없어 (수집이 끝나서 외부 호출을 안 해).
```

---

## 5. RAG 인덱스(9.5GB)를 가져갈 것인가 — 실측 후 판단

**결론: 안 가져가도 된다. 가져갈 거면 반드시 사전 예열한다.**

인덱스가 없으면 `_tool_semantic_search_ordinance` 가 경고를 남기고 폴백으로 넘어간다
(`rag 하이브리드 검색 실패 → 폴백`). 화면은 죽지 않는다.

같은 질의를 인덱스 유/무로 돌린 실측:

| 질의 | 인덱스 있음 | 인덱스 없음 |
|---|---|---|
| 야생동물 피해 보상 | 중랑구·충주시·횡성군 (관련도순) **우수** | 가평·강릉·강진 (가나다순) |
| 탄소중립 녹색성장 기본 | 김제·논산·강진 | 강릉·가평·강원 (동등) |
| 청년 월세 지원 기준 | **침수방지시설 조례** (오답) | 사회적 고립청년 지원 조례 **우수** |
| 첫 질의 소요 | **153.7초**(콜드스타트) | 0.1~0.3초 |

인덱스가 항상 낫지 않다. 재랭커가 없어 BM25 가 지배하는 구조라
"지원·기준" 같은 흔한 낱말이 들어간 질의에서 엉뚱한 조례를 올린다.

**발표 운용 지침**
- 검색 시연을 한다면 **질의를 미리 정하고 리허설**한다. 즉흥 질의는 위험하다.
- 인덱스를 가져간다면 발표 30분 전에 서버를 띄우고 **아무 질의나 한 번 던져 예열**한다
  (`run_full.bat` 은 자동 예열하고, `--no-warm` 은 예열을 건너뛴다).
- 안 가져가면 조례명 검색(`mode=name`)을 쓰거나 검색 시연을 빼고
  **조문 본문·그래프·신경망 유사도**를 보여준다. 이쪽이 훨씬 강한 장면이다.

---

## 6. 발표 당일 체크리스트

### 전날
- [ ] 노트북에 저장소 clone + `system/data/policymap.db` 배치 (4.31GB)
- [ ] `run_full.bat` 실행 → `/api/db/status` 에서 `full_edition: true` 확인
- [ ] **비행기 모드로 전 화면 1회 순회** (오프라인 실증)
- [ ] 시연할 조례 ID·검색 질의를 고정하고 리허설
- [ ] 노트북 절전/화면보호기 끄기

### 당일
- [ ] 발표 30분 전 서버 기동 (RAG 인덱스를 쓴다면 예열 질의 1회)
- [ ] 브라우저 확대 125~150% (뒷자리 가독성)
- [ ] 탭을 미리 열어둘 것 — 첫 로드가 가장 느리다

### 시연 순서 (완전판에서만 되는 것 위주)
1. **전국 요약** — 조례 199,858 / 위임 421,627 / 예산 933,527 (규모 각인)
2. **시군구 코로플레스** — 250개 폴리곤, 247개 매칭
3. **조례 상세 → 조문 본문** ← 배포본에 없는 결정적 장면 (본문 236만 건)
4. **법령 위계 그래프** — 조례에서 상위법까지 경로
5. **신경망 유사도** — 임의 조례의 유사 조례 Top-10
6. **공간자기상관** — Moran's I 0.4333, LISA 223행, BH-FDR

---

## 7. 알려진 한계 (질문받으면 이렇게 답한다)

- **`enacted_on` 은 현행 판본 공포일**이다. 연도별 건수는 확산이 아니라
  개정 활동 분포다. 확산 곡선으로 제시하지 말 것.
- **카테고리 매칭 정밀도 64.9%** (층화표본 584건, CI 60.4~69.5).
  단 신뢰도 0.8 이상 구간은 93.2% 다. 화면은 신뢰도를 함께 표기한다.
- **상위법 이름해소 605,424건은 추론**이다. 그래프에서
  `resolved_by="name-match"` 로 구분 표기된다.
- **일반구·행정시(34곳)는 자치법규 제정권이 없어** 상위 단위 값으로 채우고
  점선 테두리로 구분한다.
